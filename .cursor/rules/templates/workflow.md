# {项目名称} 工作空间流程状态

> 规则来源：`.cursor/rules/00-核心工作流程.mdc`  
> 用途：记录当前流程进度、恢复点、需加载规则、依赖文件和注意事项。  
> 维护规则：每次生成、修改、检测或编码完成后必须更新本文档。

---

## 1. 当前流程

| 字段 | 内容 |
|------|------|
| 当前阶段 | `{阶段名}` |
| 当前步骤 | `{步骤名}` |
| 当前步骤状态 | `pending` |
| 当前恢复点 | `{从哪里继续}` |
| 下一步默认动作 | `{下一步动作}` |

状态枚举：

| 状态 | 含义 |
|------|------|
| `pending` | 未开始 |
| `in_progress` | 执行中 |
| `waiting_review` | 待用户审核或确认 |
| `done` | 已完成 |
| `blocked` | 被缺失信息、冲突或外部条件阻塞 |
| `skipped` | 用户明确跳过 |

---

## 2. 当前步骤需加载规则

| 规则文件 | 用途 |
|----------|------|
| `.cursor/rules/00-核心工作流程.mdc` | 总调度、上下文加载、执行契约 |
| `{步骤规则文件}` | 当前步骤执行规则 |

---

## 3. 当前步骤依赖文件

| 文件 | 用途 | 是否必须 |
|------|------|----------|
| `{项目文件路径}` | `{为什么需要读取}` | 是 |

---

## 4. 当前步骤生成文件

| 文件 | 状态 | 说明 |
|------|------|------|
| `{项目文件路径}` | `pending` | `{产物说明}` |

---

## 5. 额外注意事项

- `{需要在当前步骤中特别注意的点}`

---

## 6. 阻塞项

| 阻塞项 | 影响 | 处理方式 |
|--------|------|----------|
| 无 | 无 | 无 |

---

## 7. 已完成阶段记录

| 阶段 | 产物 | 状态 | 备注 |
|------|------|------|------|
| `{阶段名}` | `{产物路径}` | `pending` | `{备注}` |

---

## 8. 执行记录

| 时间 | 阶段/步骤 | 操作 | 结果 |
|------|-----------|------|------|
| `{YYYY-MM-DD}` | `{阶段/步骤}` | `{执行内容}` | `{结果}` |
