---
description: Kotlin 大功能开发原则，先规划再编码，适用于多模块、多阶段联动任务
globs:
alwaysApply: false
---

# 大功能先规划再编码

> 本文件描述的是开发原则，不承担总工作流调度职责。
> 具体阶段推进仍由顶层 `00-核心工作流程.mdc` 管理。

## 一、适用场景

当任务满足以下任一条件时，应先规划：

- 牵扯多个领域模块（如订单 + 支付 + 库存 + 消息）
- 同时影响 Routes、Service、Repository、Entity、Migration
- 涉及外部 SDK / 第三方系统集成（支付、消息推送、对象存储）
- 涉及数据迁移、字段变更
- 预计需要多轮联调
- 用户明确说「先规划再写代码」

---

## 二、规划阶段至少输出

把规划写到对应的 `12-Kotlin模块开发文档.mdc` 产物（即 `projects/{currentProject}/04-后端/开发计划/{module}.md`），至少包含：

1. 功能目标与边界
2. 涉及目录、模块、表
3. 计划新增或修改的文件
4. 接口契约草案（路径、入参、出参、错误码）
5. 数据迁移计划（含字段、索引、是否需要数据回填）
6. 分步执行顺序
7. 风险点和依赖关系（外部 SDK、跨服务调用、限流）

---

## 三、进入编码前的条件

只有在以下任一条件满足时，才从规划进入编码：

- 用户明确说「开始执行计划」
- 用户明确说「按计划写代码」
- 顶层工作流明确进入编码步骤
- 接口契约已与前端模块开发文档对齐

---

## 四、编码阶段要求

- 按计划步骤推进
- 每一步完成后回写状态到 `kotlin-dev-plan.md` 与 `workflow.md`
- 发现计划失真时先更新计划，再继续编码
- 数据迁移脚本一旦写完必须本地启动验证

---

## 五、外部依赖与契约

- 若依赖第三方 SDK / 外部系统，必须先在规划阶段写 Mock 接口
- 真实环境联调前，所有 Service 必须能在 Mock 下跑通单元测试
- 第三方调用必须打日志、设置超时、设置重试策略

---

## 六、禁止事项

- 大功能一上来就横跨多层乱改
- 在计划未确认前直接铺开业务代码
- 不回写恢复点和执行记录
- 数据迁移脚本在测试环境未验证就提交到主分支
- 私自发明前端未对齐的字段或错误码
