---
description: 编程流程工作台总调度 - 收到任何输入时第一时间执行，识别当前阶段并加载对应子规则
globs: 
alwaysApply: true
---

# 编程流程工作台 - 核心调度规则

**收到任何用户输入时，必须首先执行本规则，识别当前所处阶段，再加载对应子规则。**

---

## 第一步：识别当前阶段

根据用户输入的关键词，判断属于以下哪个阶段：

| 阶段 | 触发关键词 | 加载规则 |
|------|-----------|---------|
| 需求分析 | 需求分析、分析需求、开始需求、新项目、新功能、产品文档、原型、设计稿、截图、附件 | `01-需求分析.mdc` |
| 需求文档生成 | 生成需求文档、输出需求文档、写需求文档、PRD | `02-需求文档生成.mdc` |
| 需求文档检测 | 检测需求文档、审查需求、检查需求文档、验证需求 | `03-需求文档检测.mdc` |
| 技术方案计划 | 技术方案、技术计划、技术设计、架构设计、输出方案 | `04-技术方案计划.mdc` |
| 前端Flutter文档 | Flutter、前端、dart、UI文档、前端技术文档 | `05-前端Flutter技术文档.mdc` |
| 后端Kotlin文档 | Kotlin、后端、API文档、服务端、后端技术文档 | `06-后端Kotlin技术文档.mdc` |

---

## 第二步：读取项目上下文

在加载子规则之前，先检查当前项目名称：

1. 查找工作区 `projects/` 目录下是否已有项目文件夹
2. 若用户已提供项目名称，记录为 `currentProject`
3. 若未指定项目名，在需求分析阶段第一步询问用户

**项目文档输出路径规范**：
```
projects/
└── {currentProject}/
    ├── 01-需求/
    │   ├── requirements.md          # 需求文档（PRD）
    │   ├── detection-feedback.md    # 检测阶段用户指出问题与关注点的累计记录（追加，供多轮检测）
    │   └── references/              # 用户提供的原始材料（需求分析阶段落盘）
    │       ├── README.md            # 文件索引与模块对应说明
    │       ├── images/              # 截图、原型图、流程图
    │       └── docs/                # 产品文档原文
    ├── 02-技术方案/
    │   └── tech-plan.md
    ├── 03-前端/
    │   └── flutter-tech.md
    └── 04-后端/
        └── kotlin-tech.md
```

**引用约定**：后续各阶段（PRD、技术方案、前后端文档）在引用原型或业务规则时，优先读取 `01-需求/references/` 下对应文件。

---

## 第三步：加载并执行对应子规则

读取对应的 `.cursor/rules/` 子规则文件，按其中的步骤执行。

---

## 整体工作流程

```
需求分析 → 需求文档生成 → 需求文档检测（可多轮迭代）→ 技术方案计划 → Flutter技术文档 + Kotlin技术文档
```

- **需求文档检测阶段会暂停**：检测完成后等待用户确认（"文档通过" / "继续下一步"）再推进
- **每个阶段独立触发**：用户可以随时通过关键词跳转到任意阶段
- **文档按项目隔离**：不同项目的文档存放在各自的 `projects/{项目名}/` 目录下
