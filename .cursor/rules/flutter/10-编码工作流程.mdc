---
description: Flutter 编码工作流程规则，仅用于进入 Flutter 代码阶段后的执行、验证与回写，不负责整体项目流程调度
globs: "**/lib/**/*.dart,**/pubspec.yaml,**/test/**/*.dart"
alwaysApply: false
---

# 编码工作流程

> 本规则只负责 **Flutter 编码阶段内部** 的执行流程。
> 它不是整体项目流程，不负责“需求 → 文档 → 技术方案 → 开发计划”的总调度。
> 整体项目阶段推进仍然由顶层 `00-核心工作流程.mdc` 负责。

---

## 一、适用范围

本规则在以下场景生效：

- 用户要求开始 Flutter 编码
- 用户要求继续修改 Flutter 代码
- 用户要求调试、修复、联调 Flutter 工程
- 当前 `workflow.md` 的当前阶段已经进入 `Flutter编码`

不适用场景：

- 需求分析
- PRD 生成或修订
- 技术方案生成
- Flutter 技术文档生成
- Flutter 总体开发计划或模块开发文档生成

---

## 二、与整体项目流程的边界

### 2.1 整体项目流程负责什么

整体项目流程由顶层规则负责：

- 当前在哪个阶段
- 是否允许进入下一阶段
- 是否需要暂停等待用户确认
- 是否需要回到文档阶段

### 2.2 Flutter 编码流程负责什么

本规则只负责：

- 进入 Flutter 代码阶段后，该先读哪些文档
- 当前编码步骤应该怎么拆解
- 代码实现顺序
- 校验顺序
- 状态回写顺序

---

## 三、编码前必须读取的内容

进入 Flutter 编码前，至少读取以下内容：

1. `projects/{currentProject}/workflow.md`
2. `projects/{currentProject}/03-前端/flutter-tech.md`
3. `projects/{currentProject}/03-前端/flutter-dev-plan.md`
4. 当前恢复点对应的模块文档
5. 若有 UI 任务，还需读取：
   - `projects/{currentProject}/01-需求/figma-design-plan.md`
   - `projects/{currentProject}/01-需求/references/images/`
6. 若有接口或状态流转任务，还需读取：
   - `projects/{currentProject}/04-后端/java-tech.md`
7. 若技术文档指定参考工程，还需读取参考工程的真实结构和入口文件
8. 若当前任务包含测试、回归、冒烟或自动化验证，还需读取：
   - `.cursor/rules/flutter/11-测试规则.mdc`

若目标 Flutter 工程已经初始化，功能开发时必须优先读取工程内 `.cursor/rules/`。项目规则与总体规则冲突时，以项目规则为准。

---

## 四、编码阶段的执行顺序

进入具体 Flutter 编码后，默认按以下顺序执行：

### 4.1 先确认当前恢复点

从 `workflow.md` 与 `flutter-dev-plan.md` 确认：

- 当前步骤编号
- 当前步骤状态
- 当前恢复点文件
- 是否已有 `in_progress` 的编码任务

规则：

- 优先继续当前 `in_progress` 步骤
- 没有 `in_progress` 时，再从第一个依赖满足的 `pending` 步骤继续
- 不得跳过前置依赖直接写后续功能

### 4.2 再做当前步骤的最小实现计划

开始写代码前，先明确本轮只做什么：

- 要改哪些文件
- 要补哪层内容
- 哪些事情本轮不做

规则：

- 编码阶段仍然按“当前步骤”推进
- 不因为顺手就跨多个步骤大面积扩写

### 4.3 按层次实现

默认实现顺序：

1. 结构与目录
2. 模型 / 常量 / 路由名
3. Service / Store / 持久化
4. Controller
5. View / Widgets / Dialog
6. 页面状态补齐
7. 联调与验证

### 4.4 验证

至少做：

- `flutter analyze`
- 必要的 `flutter test`
- 若是 UI 或链路改动，做最小链路验证

### 4.5 回写

编码完成后至少回写：

- `workflow.md`
- `flutter-dev-plan.md`（如任务状态已变化）
- 对应模块开发文档（如模块状态或边界已变化）

---

## 五、编码子流程模式

进入 Flutter 编码模块后，只存在两种顶层模式：

| 模式 | 进入条件 | 负责内容 |
|------|----------|----------|
| 项目初始化 | 项目缺少 `.cursor/rules/` Flutter 项目规则，或缺少基础工程结构 | 复制项目规则模板，搭建基础工程结构 |
| 功能开发 | 项目已初始化 | 新功能、UI 调整、bug 修复、逻辑修改、重构、其他代码任务 |

项目初始化不是普通功能开发任务。初始化完成后，控制权回到 `13-Flutter编码索引.mdc`，并继续进入功能开发循环；除非初始化阻塞或用户明确暂停，不直接回主流程。

### 5.1 项目初始化模式

初始化模式只允许做：

- 把总体 Flutter 规则模板复制到项目 `.cursor/rules/`
- 搭建 `main/global/theme/routes/services/store/utils/widgets/pages` 基础结构
- 产出最小可运行壳页

初始化模式不允许夹带业务页面、UI 对稿、bug 修复、业务逻辑修改或未确定 SDK 接入。

### 5.2 功能开发循环

功能开发是编码模块内部循环。每轮循环先加载项目内 `.cursor/rules/`，再按用户输入和当前修改范围判断任务类型：

| 功能开发任务类型 | 规则文件 | 使用场景 |
|------------------|----------|----------|
| 新功能开发 | 项目规则 `task-new_feature.mdc`，必要时总体规则 `flutter/07-新功能开发.mdc` | 新建页面模块、子功能、弹层、路由、Controller |
| UI 处理 | 项目规则 `task-ui_task.mdc` + 项目规则 `08-UI处理任务.mdc` | 对稿、视觉调整、布局修复、状态 UI 补齐 |
| Bug 修复 | 项目规则 `task-bug_fix.mdc` | 崩溃、报错、行为异常、测试失败 |
| 逻辑修改 | 项目规则 `task-logic_change.mdc` | 改业务规则、状态流转、接口调用顺序 |
| 重构 | 项目规则 `task-refactor.mdc` | 不改变行为的结构整理、抽方法、拆类、去重复 |
| 其他 | 项目规则 `task-other.mdc` | 不能明确归类的 Flutter 代码任务 |

默认规则：

- 首轮编码或模块开发默认按「新功能开发」处理。
- 多轮交互或返工时，再按实际问题细分为 UI、bug、逻辑修改、重构或其他。
- 功能开发任务规则只在功能开发循环内辅助判断任务类型；初始化不通过 `task-project_init.mdc` 分派。
- 若功能开发任务规则与项目规则、模块文档或技术方案冲突，以项目规则、模块文档和技术方案为准。

### 5.3 UI 任务规则

只要本轮涉及 UI：

- 必须读取项目内 `08-UI处理任务.mdc`。
- 必须读取 Figma frame 或已沉淀的设计图。
- Figma 无法通信且没有替代设计材料时，返回 `blocked`。
- 不允许在不知道 UI 的情况下凭空实现界面。

### 5.4 功能开发实现顺序

新功能或模块开发默认按以下顺序：

1. 模型 / 参数
2. 真实 API 静态方法
3. Store / Service
4. Controller
5. Controller 私有 mock / real 分支
6. View / Widgets / Dialog
7. 状态补齐
8. analyze / test
9. 记录结果

---

## 六、阻塞式编码阶段规则

Flutter 编码属于阻塞式代码开发阶段。

含义：

- 只要用户没有明确说“进入下一步”
- 就默认还停留在当前 Flutter 编码步骤
- 默认继续做当前步骤内的修改、调试、验证和回写

规则：

- 不自动把当前编码步骤标成完成并切下一步
- 不自动从 Flutter 编码跳到后端开发或前端接接口
- 只有用户明确推进，或当前步骤已经闭合并需要用户确认时，才允许切换

---

## 七、文档与代码冲突时的优先级

当发现冲突时，处理顺序如下：

1. 当前版本的 `figma-design-plan.md`
2. 当前版本的 `flutter-tech.md`
3. 当前版本的 `flutter-dev-plan.md`
4. 当前恢复点模块文档
5. 现有实现代码

规则：

- 若冲突较大，先修文档再修代码
- 不要直接在代码里私自发明新口径

---

## 八、完成判定

当前 Flutter 编码步骤要满足以下条件，才可认为本轮完成：

- 本轮目标代码已落地
- 分析和测试已通过或已明确说明无法执行原因
- 当前步骤相关文档已回写
- 当前步骤的阻塞与风险已记录

若以上任一缺失，则本轮仍视为“未完全闭合”。

---

## 九、禁止事项

- 不读取恢复点文档就直接改代码
- 不按当前步骤推进，随意跨步实现
- 不验证就宣称完成
- 不回写 `workflow.md`
- 把 Flutter 编码流程误当成整体项目流程
