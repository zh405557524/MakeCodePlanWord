---
description: 编程流程工作台总调度 - 收到任何输入时第一时间执行，识别当前阶段、读取工作空间状态并加载对应子规则
globs:
alwaysApply: true
---

# 编程流程工作台 - 核心调度规则

**收到任何用户输入时，必须首先执行本规则。总调度只负责识别阶段、定位工作空间、读取状态记录、加载对应规则和依赖文件，不直接处理具体步骤产物。**

---

## 第零步：明确规则层与工作空间层

本工作台分为三层：

1. **总调度规则**：`00-核心工作流程.mdc`，只控制阶段识别、上下文加载、流程推进和门禁。
2. **步骤规则**：`01-需求分析.mdc` 到 `08-Flutter模块开发文档.mdc` 及 `flutter/` 下编码规则，只描述某一步如何生成或修改对应产物。
3. **工作空间**：`projects/{currentProject}/`，保存具体项目产物、流程状态、依赖文件、注意事项和变更计划。

边界要求：

- `.cursor/rules/` 只保存抽象规则，不写入具体项目业务状态。
- `projects/{currentProject}/` 才保存具体任务、生成文件、恢复点和项目级注意事项。
- 总流程像责任链调度器，只决定“进入哪一步”和“加载哪些文件”；具体规则只在进入对应步骤后生效。
- 进入下一步后，不依赖上一轮对话上下文；必须以 `workflow.md` 和已生成文件作为恢复来源。

---

## 第一步：识别当前阶段

根据用户输入关键词判断阶段。若关键词同时命中多个阶段，以用户明确表达的目标为准；若仍不明确，优先读取 `workflow.md` 的当前恢复点。

| 阶段 | 触发关键词 | 加载规则 |
|------|-----------|---------|
| 需求分析 | 需求分析、分析需求、开始需求、新项目、新功能、产品文档、原型、设计稿、截图、附件 | `01-需求分析.mdc` |
| 需求文档生成 | 生成需求文档、输出需求文档、写需求文档、PRD | `02-需求文档生成.mdc` |
| 需求文档检测 | 检测需求文档、审查需求、检查需求文档、验证需求 | `03-需求文档检测.mdc` |
| Figma设计稿计划 | figma设计稿计划、设计稿计划、生成设计计划、生成figma计划、生成 Figma 设计稿计划 | `03A-Figma设计稿计划.mdc` |
| 技术方案计划 | 技术方案、技术计划、技术设计、架构设计、输出方案 | `04-技术方案计划.mdc` |
| 前端Flutter文档 | Flutter、前端、dart、UI文档、前端技术文档 | `05-前端Flutter技术文档.mdc` |
| 后端Kotlin文档 | Kotlin、后端、API文档、服务端、后端技术文档 | `06-后端Kotlin技术文档.mdc` |
| Flutter总体开发计划 | Flutter开发计划、总体开发计划、开发计划文档、编码计划、开发计划 | `07-Flutter开发计划文档.mdc` |
| Flutter模块开发文档 | 模块开发文档、模块开发计划、Flutter模块开发文档 | `08-Flutter模块开发文档.mdc` |
| Flutter编码 | 生成代码、输出项目代码、实现页面、继续编码、搭建工程、接入前端、接入后端 | 当前阶段规则 + `flutter/` 下相关编码规则 |

---

## 第二步：定位工作空间并读取流程状态

在加载子规则之前，先确定 `currentProject` 并读取工作空间状态：

1. 查找工作区 `projects/` 目录下已有项目文件夹。
2. 若用户已提供项目名称，记录为 `currentProject`。
3. 若用户未指定项目名：
   - 若只有一个项目，默认使用该项目。
   - 若有多个项目且无法从上下文判断，必须询问用户。
   - 若处于需求分析新项目阶段，先询问项目名。
4. 定位 `projects/{currentProject}/workflow.md`。
5. 若 `workflow.md` 不存在，先按 `.cursor/rules/templates/workflow.md` 创建项目级状态文件，再继续执行。
6. 若本轮是二次调整、局部修改、返工或修订，定位 `projects/{currentProject}/change-plan.md`；不存在时按 `.cursor/rules/templates/change-plan.md` 创建。

**项目文档输出路径规范**：

```text
projects/
└── {currentProject}/
    ├── workflow.md                 # 工作空间流程状态、恢复点、依赖文件、注意事项
    ├── change-plan.md              # 二次调整计划，仅发生变更时创建或更新
    ├── 01-需求/
    │   ├── requirements.md          # 需求文档（PRD）
    │   ├── detection-feedback.md    # 检测阶段用户指出问题与关注点的累计记录
    │   ├── figma-design-plan.md     # Figma 设计稿计划
    │   └── references/
    │       ├── README.md
    │       ├── images/
    │       └── docs/
    ├── 02-技术方案/
    │   └── tech-plan.md
    ├── 03-前端/
    │   ├── flutter-tech.md
    │   ├── flutter-dev-plan.md
    │   └── 开发计划/
    └── 04-后端/
        └── kotlin-tech.md
```

---

## 第三步：按 `workflow.md` 加载上下文

加载上下文时遵守最小充分原则：

1. 必须读取本核心规则。
2. 必须读取当前阶段对应的步骤规则。
3. 必须读取 `projects/{currentProject}/workflow.md`。
4. 只读取 `workflow.md` 中“当前步骤依赖文件”列出的项目文件。
5. 若步骤规则明确要求额外文件，再补充读取。
6. 不把上一阶段对话当成上下文来源；上一阶段的结论必须已经沉淀到项目文件。

**引用约定**：

- 需求之后的阶段引用业务规则时，优先读取 `01-需求/requirements.md`。
- UI、页面、状态、组件与交互约束优先读取 `01-需求/figma-design-plan.md`；若缺失，必须在 `workflow.md` 的“阻塞项/注意事项”中记录。
- 技术方案、前后端文档、开发计划与编码阶段应按 `workflow.md` 的依赖文件加载，不因单次小改动扩大读取和重写范围。

---

## 第四步：统一执行契约

所有步骤规则都必须遵守以下契约。

### 4.1 执行前

1. 确认 `currentProject`、当前阶段、当前步骤状态和恢复点。
2. 读取当前步骤规则和 `workflow.md` 中列出的依赖文件。
3. 若是二次调整，先读取或创建 `change-plan.md`，明确影响文件与不应改动范围。
4. 若依赖文件缺失或冲突，先更新 `workflow.md` 的阻塞项并停止推进。

### 4.2 执行中

1. 先生成本轮执行计划，再按计划生成或修改文件。
2. 只处理当前步骤规则负责的产物，不跨步骤顺手重写其他文档。
3. 小改动默认只改 `change-plan.md` 标出的影响文件。
4. 除非用户明确要求重构，否则禁止因局部调整触发大面积重写。

### 4.3 执行后

1. 更新 `workflow.md`：
   - 当前阶段
   - 当前步骤状态
   - 当前恢复点
   - 当前步骤需加载规则
   - 当前步骤依赖文件
   - 当前步骤生成文件
   - 额外注意事项
   - 阻塞项
   - 下一步默认动作
   - 执行记录
2. 若本轮有二次调整，更新 `change-plan.md` 的完成记录。
3. 到达暂停点时必须等待用户确认，不自动跳入下一阶段。

状态枚举固定为：

| 状态 | 含义 |
|------|------|
| `pending` | 未开始 |
| `in_progress` | 执行中 |
| `waiting_review` | 待用户审核或确认 |
| `done` | 已完成 |
| `blocked` | 被缺失信息、冲突或外部条件阻塞 |
| `skipped` | 用户明确跳过 |

---

## 第五步：二次调整与局部变更规则

当用户要求修改已生成内容时，默认按局部变更处理：

1. 先生成或更新 `projects/{currentProject}/change-plan.md`。
2. 变更类型默认写为 `small_change`。
3. 明确本次影响文件、无需修改范围、执行步骤和验收标准。
4. 若 UI 改一小段逻辑，只追踪可能受影响的需求、产品文档、技术文档或代码文件；不得整篇重写。
5. 只有用户明确说“重构、整体重写、重新设计、推倒重来”等，才允许将变更类型改为 `refactor` 或 `rewrite`。
6. 执行完成后，把改动摘要回写到 `workflow.md` 和 `change-plan.md`。

---

## 第六步：代码生成前架构门禁

当用户要求“生成代码、输出项目代码、实现页面、继续编码、搭建工程、接入前端/后端工程”时，必须在写任何业务代码之前执行架构门禁。

### 6.1 必须先读取的上下文

编码前必须先读取并对齐以下内容：

1. `projects/{currentProject}/workflow.md`。
2. 当前阶段对应的 `.cursor/rules/` 子规则。
3. 若是 Flutter 工程，必须读取 `.cursor/rules/flutter/08-项目结构目录.mdc`、`task-project_init.mdc`、`task-new_feature.mdc` 或 `task-ui_task.mdc` 中与当前任务相关的规则。
4. 当前项目的技术文档与开发计划，例如：
   - `projects/{currentProject}/03-前端/flutter-tech.md`
   - `projects/{currentProject}/03-前端/flutter-dev-plan.md`
   - `projects/{currentProject}/03-前端/开发计划/*.md`
   - 若存在后端接口文档，也必须读取 `projects/{currentProject}/04-后端/kotlin-tech.md`
5. 若技术文档指定参考工程，例如 `G:\code\soul\Solfeggio`，必须先检查参考工程真实目录结构和入口文件，再生成代码。

### 6.2 必须先确认架构清单

写代码前必须形成并遵守架构清单。以 Flutter 为例，若项目规则或技术文档要求如下结构，则必须先搭好该结构，再写 UI 和业务：

```text
lib/
├── apis/
├── components/
├── enums/
├── models/
├── pages/{feature}/view.dart + controller.dart + index.dart
├── routes/name.dart + router.dart + observers.dart + index.dart
├── services/
├── store/
├── utils/
├── widgets/
├── global.dart
├── main.dart
└── theme.dart
```

禁止在已有明确项目规则时，先生成单文件原型，或使用未被规则/技术文档认可的通用架构，例如随意新增 `app/`、`core/`、`controllers/` 等目录来替代项目标准骨架。

### 6.3 编码后的架构验收

代码生成完成后，除了运行 `flutter analyze`、`flutter test` 等编译测试外，还必须做架构验收：

1. 扫描 `lib/` 顶层目录，确认与项目规则一致。
2. 扫描 `pages/`，确认业务页面均位于 `pages/{feature}/`，并包含 `view.dart`、`controller.dart`、`index.dart`。
3. 确认入口、路由、全局初始化、主题文件符合规则，例如 `main.dart`、`global.dart`、`routes/`、`theme.dart`。
4. 若发现架构不一致，必须先修正架构，再向用户汇报完成。
5. 更新 `workflow.md` 中的恢复点、执行记录和下一步。

---

## 整体工作流程

```text
需求分析 → 需求文档生成 → 需求文档检测（可多轮迭代）→ Figma设计稿计划 → 技术方案计划 → Flutter技术文档 → Flutter总体开发计划 → 审核 → Flutter模块开发文档 → Flutter编码
```

- **需求文档检测阶段会暂停**：检测完成后等待用户确认（"文档通过" / "继续下一步"）再进入 Figma 设计稿计划阶段。
- **Figma设计稿计划阶段会暂停**：设计计划生成后等待用户按计划补齐设计稿或明确确认可继续，再进入技术方案阶段。
- **Flutter总体开发计划阶段会暂停**：总体开发计划生成后等待用户审核，通过后再生成模块开发文档。
- **每个阶段独立触发**：用户可以随时通过关键词跳转到任意阶段，但必须更新 `workflow.md`。
- **文档按项目隔离**：不同项目的文档存放在各自的 `projects/{项目名}/` 目录下。
