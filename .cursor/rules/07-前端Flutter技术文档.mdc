---
description: 前端 Flutter 技术文档阶段 - 基于技术方案、工时评估与可选参考工程生成 Flutter 专项文档
globs:
alwaysApply: false
---

# 前端 Flutter 技术文档规则

## 通用执行契约

执行本规则前，必须先完成核心规则中的工作空间定位：

- 读取 `projects/{currentProject}/workflow.md`；若不存在，先按 `.cursor/rules/templates/workflow.md` 创建。
- 读取本规则，以及 `workflow.md` 中“当前步骤依赖文件”列出的材料。
- 执行前先形成本轮前端技术文档计划，再生成或更新 Flutter 技术文档。
- 执行后更新 `workflow.md` 的当前阶段、状态、恢复点、依赖文件、生成文件、注意事项和执行记录。
- 若本轮是二次调整或命中变更管控关键词，先读取或创建 `projects/{currentProject}/change-plan.md`，默认按 `small_change` 限定影响范围。

## 前置条件

执行本规则前，必须确认：
1. `projects/{currentProject}/02-技术方案/tech-plan.md` 已存在
2. `projects/{currentProject}/02-技术方案/effort-estimate.md` 已存在或用户明确跳过
3. `projects/{currentProject}/01-需求/figma-design-plan.md` 已存在
4. 用户已触发「生成 Flutter 技术文档」或类似指令

## 参考工程

参考工程不再硬编码到规则中，而是由项目级配置驱动：

- `workflow.md` 中的 `referenceProject` 字段指向真实的参考工程绝对路径（如已设置）。
- 若未设置 `referenceProject`，则不引用任何外部工程，直接按本规则与 `.cursor/rules/flutter/` 通用规则库生成文档。
- 若设置了 `referenceProject` 但目录不存在，必须在 `workflow.md` 的「阻塞项」中记录，并继续按通用规则库执行，不得伪造引用。

参考工程典型可吸收的工程约定（仅作启发，最终以 `referenceProject` 真实结构为准）：

- `main.dart + Global.init()`
- `GetX`
- `GetStorage + Hive`
- `MaterialApp.router + go_router`
- `flutter_screenutil`
- `CustomScaffold`
- `pages/{feature}/index.dart + controller.dart + view.dart`

## 规则来源（生成前必读）

- `.cursor/rules/flutter/01-项目初始化规则.mdc`
- `.cursor/rules/flutter/02-项目结构目录.mdc`
- `.cursor/rules/flutter/03-编码规范.mdc`
- `.cursor/rules/flutter/04-导航规则.mdc`
- `.cursor/rules/flutter/05-多语言支持.mdc`
- `.cursor/rules/flutter/06-第三方依赖参考.mdc`
- `.cursor/rules/flutter/07-新功能开发.mdc`
- `.cursor/rules/flutter/08-UI处理任务.mdc`
- `.cursor/rules/flutter/09-大功能先规划再编码.mdc`
- `.cursor/rules/flutter/10-编码工作流程.mdc`
- `.cursor/rules/flutter/11-测试规则.mdc`

## Flutter 规则目录的定位

`.cursor/rules/flutter/` 是一个**通用规则库 + 代码模板库**：

1. 可以直接作为新 Flutter 工程的初始化规则源
2. 可以直接作为生成 `flutter-tech.md` 时的规则输入
3. 其中的规则默认是通用版本，不随单个项目反复改写

因此，本规则负责的不是“修改 `.cursor/rules/flutter`”，而是：

- 读取这套通用规则库
- 结合当前项目业务、Figma 与技术方案
- 输出当前项目自己的 Flutter 开发规则和技术文档口径

## 输出路径

- `projects/{currentProject}/03-前端/flutter-tech.md`

## UI 事实来源

生成 Flutter 技术文档时，UI 与交互事实至少按以下顺序综合判断：

1. `projects/{currentProject}/01-需求/requirements.md`
2. `projects/{currentProject}/01-需求/figma-design-plan.md`
3. `projects/{currentProject}/01-需求/references/images/`
4. `projects/{currentProject}/01-需求/references/docs/` 中的颜色与视觉文档

## 文档要求

生成 `flutter-tech.md` 时必须明确：

1. 当前 Flutter 最终技术路线
2. 当前项目从通用规则库中继承了哪些项目初始化规则
3. 当前项目结构与参考工程对齐点（若有 `referenceProject`）
4. 当前项目第三方依赖基线
5. 启动与全局初始化
6. 状态管理与数据流
7. 存储设计（GetStorage / Hive）
8. 路由配置
9. UI 基础规范
10. 页面实现摘要
11. 编码任务清单
12. 编码注意事项
13. Flutter 编码工作流程
14. 测试用例与自动化辅助规则

还必须单独输出“项目专属开发规则”章节，至少说明：

- 继承了哪些通用规则
- 哪些通用规则在当前项目中被裁剪
- 哪些规则因为当前业务、Figma 或技术方案被特化
- 当前项目最终执行的导航、编码规范、多语言、UI、规划与编码关系
- 当前项目最终执行的 Flutter 编码推进流程
- 当前项目最终执行的测试用例结构、测试人员编写约束，以及 Python 自动化辅助执行规则

也就是说：

- **如何输出规则，只在本文件中定义**
- `.cursor/rules/flutter/` 只保存通用规则和模板，不在里面写“如何生成项目规则”

其中 UI 基础规范、页面实现摘要、编码任务清单必须明确引用 `figma-design-plan.md` 中的页面、状态、组件和交互要求，而不是只按散图兜底。

## 技术口径

- 默认使用 `GetX`
- 默认使用 `GetStorage + Hive`
- 不再沿用 `Riverpod + Drift` 作为主路线
- 若技术方案与参考工程冲突，优先保持参考工程风格，并在文档中写明
- 若设计计划与单张参考图存在差异，优先以 `figma-design-plan.md` 约束页面与状态要求，并注明图中现状
- 若通用规则库与当前业务存在差异，优先保留通用规则，再在 `flutter-tech.md` 中明确写出项目级特化点

## 推进规则

- 文档生成完成后，下一步优先进入 09 Flutter 总体开发计划阶段
- 后端 Kotlin 技术文档（08）可与本文档并行生成，两者完成后再分别进入 09/10 总体计划阶段
