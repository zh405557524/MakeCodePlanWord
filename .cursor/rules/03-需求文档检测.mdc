---
description: 需求文档检测阶段 - 综合 PRD、references 与用户反馈进行检测；用户指出的问题落盘供多轮使用
globs: 
alwaysApply: false
---

# 需求文档检测规则

## 重要：本阶段必须暂停

**检测完成后，必须输出检测报告并等待用户明确确认，才能进入下一阶段。**  
不得自动推进到技术方案阶段。

---

## 检测输入源（须综合考量）

本阶段**不得只读 PRD**。须同时纳入：

| 来源 | 路径 / 说明 |
|------|-------------|
| PRD | `projects/{currentProject}/01-需求/requirements.md` |
| 原始产品文档 | `projects/{currentProject}/01-需求/references/docs/` 下可读文本（如 `product-doc.md`） |
| 材料索引 | `projects/{currentProject}/01-需求/references/README.md`（若存在） |
| 原型/截图 | `projects/{currentProject}/01-需求/references/images/`：至少对照文件名与 README 说明；若对话或上下文中可查看图片内容，须与 PRD 第 3 章 UI 结构核对 |
| 用户累计反馈 | `projects/{currentProject}/01-需求/detection-feedback.md`（若存在） |
| 本轮对话 | 用户在本轮检测对话中**新提出**的问题、质疑、补充 → **必须先落盘**（见第一步） |

**原则**：检测结果须说明 PRD 与 **references**、**detection-feedback** 是否一致，有无遗漏或未闭环项。

---

## 执行步骤

### 第一步：同步用户反馈并收集材料

1. **若用户在本轮消息中指出了新问题、质疑或补充条件**（含对 PRD、原型、文档的意见）：
   - 将要点**追加**写入：  
     `projects/{currentProject}/01-需求/detection-feedback.md`
   - 若文件不存在，先创建；文件顶部保留固定说明段，下方为「历史记录」；每条记录使用三级标题 `### {日期时间} 第{n}轮`（或「用户补充」），下列表归纳用户原文要点（可适度合并去重，勿曲解）。
2. 读取 `requirements.md` 全文。
3. 读取 `references/docs/` 下相关文档、`references/README.md`（若有）。
4. 列出 `references/images/` 下文件，并结合 README 与可见图片内容（若有）理解界面与流程。
5. 读取 `detection-feedback.md` 全文（含刚追加内容）。

---

### 第二步：执行质量检测

按以下 **10** 个维度逐一检测，每项给出 通过 / 警告 / 缺失 三种状态：

#### 检测清单

**1. 完整性检测**
- [ ] 是否有项目背景和目标描述
- [ ] 是否列出了所有核心功能模块
- [ ] 每个功能是否有详细描述
- [ ] 是否有技术约束说明
- [ ] 是否有里程碑规划

**2. 用户故事检测**
- [ ] 核心功能是否都有对应的用户故事
- [ ] 用户故事格式是否规范（作为/我希望/以便）
- [ ] 是否从用户视角描述而非技术视角

**3. 验收标准检测**
- [ ] 每个功能是否有可验证的验收标准
- [ ] 验收标准是否具体、可量化
- [ ] 是否覆盖了异常/边界情况

**4. UI 页面流检测**
- [ ] 是否有完整的页面流转描述（从开屏到各主要页面）
- [ ] 全局导航结构是否清晰（底部 Tab / 侧边栏等）
- [ ] 每个主要页面是否有区块结构说明（顶部/中间/底部区域）
- [ ] 页面的入口和出口是否都有描述
- [ ] 可交互元素及其触发行为是否有说明
- [ ] **与 references 中图片/说明对照**：PRD 是否覆盖图中主要页面与区块；明显遗漏或冲突须标出

**5. 前后端交互检测**
- [ ] 涉及网络请求的核心功能是否都有交互链路描述
- [ ] 每条交互是否包含：操作步骤 + API 路径与入参 + 后端响应 + 前端处理
- [ ] 是否覆盖了主要异常流程（网络错误、业务错误）
- [ ] 加载状态（loading/toast 等）是否有说明
- [ ] **与 references 文档对照**：文档中写明的接口/流程是否在 PRD 第 4 章有对应描述

**6. 技术可行性检测**
- [ ] 技术约束是否与 Flutter + Kotlin 技术栈对应
- [ ] 是否有明显的技术矛盾或无法实现的需求
- [ ] 非功能性需求是否合理（性能指标是否现实）

**7. 歧义检测**
- [ ] 是否存在模糊描述（如"高性能"、"用户友好"等未量化表述）
- [ ] 是否有相互矛盾的需求
- [ ] 优先级是否明确

**8. 遗漏检测**
- [ ] 若产品需要账号、权限或类似基础能力，PRD 是否已覆盖（勿强行要求无此需求的产品写登录等）
- [ ] 是否说明了数据存储方案
- [ ] 是否考虑了错误处理和异常流程
- [ ] **与 references 对照**：产品文档或图片中已出现的能力/页面是否在 PRD 中有体现

**9. 里程碑检测**
- [ ] 阶段划分是否合理
- [ ] MVP 范围是否清晰

**10. 用户反馈与关注点闭环检测**
- [ ] 读取 `detection-feedback.md` 中**全部历史条目**（若文件不存在或为空则标 N/A）
- [ ] 每条用户曾指出的问题：PRD **是否已修改落实**、**是否仍待处理**、或**已明确拒绝/延后**（须在报告中列清单）
- [ ] 本轮用户新写入反馈是否已体现在本条检测结论中

---

### 第三步：输出检测报告

按以下格式输出检测报告（须包含「原始材料与用户反馈对照」小节）：

```
需求文档检测报告 - {currentProject}

检测时间：{当前时间}
文档版本：v{当前版本}
总体评分：{通过数}/{总检测项数}

---

检测结果汇总

| 维度           | 状态         | 说明 |
|--------------|------------|------|
| 完整性          | 通过/警告/缺失   | |
| 用户故事         | 通过/警告/缺失   | |
| 验收标准         | 通过/警告/缺失   | |
| UI页面流        | 通过/警告/缺失   | |
| 前后端交互        | 通过/警告/缺失   | |
| 技术可行性        | 通过/警告/缺失   | |
| 歧义           | 通过/警告/缺失   | |
| 遗漏           | 通过/警告/缺失   | |
| 里程碑          | 通过/警告/缺失   | |
| 用户反馈闭环       | 通过/警告/缺失   | |

---

原始材料与用户反馈对照

【与 references】
- 文档要点与 PRD 一致 / 不一致处：…
- 图片（或说明）与 PRD 第 3 章一致 / 遗漏：…

【与 detection-feedback.md】
- 已闭环项：…
- 仍待 PRD 响应项：…
- 本轮新写入反馈摘要：…

---

问题详情

【必须修复（阻塞继续）】
1. {问题描述} → 建议：{修改建议}

【建议优化（可选修复）】
1. {问题描述} → 建议：{修改建议}

---

结论

PASS：文档质量良好，可进入技术方案阶段。
FAIL：存在必须修复的问题，请按上方建议修改后重新检测。
```

---

### 第四步：强制暂停等待确认

输出报告后，**必须停止**，等待用户的明确指令：

**若检测结果为 PASS**：
> 检测通过！请输入以下指令之一继续：
> - `"文档通过，生成技术方案"` — 直接进入技术方案阶段
> - `"我要修改需求"` — 描述修改内容后重新生成文档（你的意见可继续写入 detection-feedback.md）

**若检测结果为 FAIL**：
> 文档存在必须修复的问题，请：
> - 描述修改内容，我来更新文档并重新检测（**你指出的问题会追加到** `detection-feedback.md`）
> - 或输入 `"忽略问题，强制通过"` 跳过检测（不推荐）

---

### 第五步：迭代处理

用户提出修改意见后：

1. **将用户本轮修改意见要点追加到** `projects/{currentProject}/01-需求/detection-feedback.md`（若第一步已写入可合并去重，避免完全重复段落）。
2. 更新 `projects/{currentProject}/01-需求/requirements.md`（同时更新版本号 v1.x）。
3. 重新执行检测流程（回到第一步）。
4. 输出新版检测报告。

**多轮迭代记录**：每次更新在 `requirements.md` 顶部更新版本号和修改日志；`detection-feedback.md` 保留完整追问历史，便于审计与回归检测。
