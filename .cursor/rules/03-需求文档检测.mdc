---
description: 需求文档检测阶段 - 对 PRD 进行质量检测，暂停等待用户确认，支持多轮迭代修改
globs: 
alwaysApply: false
---

# 需求文档检测规则

## ⚠️ 重要：本阶段必须暂停

**检测完成后，必须输出检测报告并等待用户明确确认，才能进入下一阶段。**  
不得自动推进到技术方案阶段。

---

## 执行步骤

### 第一步：读取 PRD 文档

读取 `projects/{currentProject}/01-需求/requirements.md` 的完整内容。

---

### 第二步：执行质量检测

按以下 7 个维度逐一检测，每项给出 ✅通过 / ⚠️警告 / ❌缺失 三种状态：

#### 检测清单

**1. 完整性检测**
- [ ] 是否有项目背景和目标描述
- [ ] 是否列出了所有核心功能模块
- [ ] 每个功能是否有详细描述
- [ ] 是否有技术约束说明
- [ ] 是否有里程碑规划

**2. 用户故事检测**
- [ ] 核心功能是否都有对应的用户故事
- [ ] 用户故事格式是否规范（作为/我希望/以便）
- [ ] 是否从用户视角描述而非技术视角

**3. 验收标准检测**
- [ ] 每个功能是否有可验证的验收标准
- [ ] 验收标准是否具体、可量化
- [ ] 是否覆盖了异常/边界情况

**4. 技术可行性检测**
- [ ] 技术约束是否与 Flutter + Kotlin 技术栈对应
- [ ] 是否有明显的技术矛盾或无法实现的需求
- [ ] 非功能性需求是否合理（性能指标是否现实）

**5. 歧义检测**
- [ ] 是否存在模糊描述（如"高性能"、"用户友好"等未量化表述）
- [ ] 是否有相互矛盾的需求
- [ ] 优先级是否明确

**6. 遗漏检测**
- [ ] 是否遗漏了登录/注册/权限等基础功能
- [ ] 是否说明了数据存储方案
- [ ] 是否考虑了错误处理和异常流程

**7. 里程碑检测**
- [ ] 阶段划分是否合理
- [ ] MVP 范围是否清晰

---

### 第三步：输出检测报告

按以下格式输出检测报告：

```markdown
## 需求文档检测报告 - {currentProject}

**检测时间**：{当前时间}  
**文档版本**：v{当前版本}  
**总体评分**：{通过数}/{总检测项数}

---

### 检测结果汇总

| 维度 | 状态 | 说明 |
|------|------|------|
| 完整性 | ✅/⚠️/❌ | |
| 用户故事 | ✅/⚠️/❌ | |
| 验收标准 | ✅/⚠️/❌ | |
| 技术可行性 | ✅/⚠️/❌ | |
| 歧义 | ✅/⚠️/❌ | |
| 遗漏 | ✅/⚠️/❌ | |
| 里程碑 | ✅/⚠️/❌ | |

---

### 问题详情

#### ❌ 必须修复（阻塞继续）
1. {问题描述} → 建议：{修改建议}

#### ⚠️ 建议优化（可选修复）
1. {问题描述} → 建议：{修改建议}

---

### 结论

{PASS：文档质量良好，可进入技术方案阶段。}
{FAIL：存在必须修复的问题，请按上方建议修改后重新检测。}
```

---

### 第四步：⛔ 强制暂停等待确认

输出报告后，**必须停止**，等待用户的明确指令：

**若检测结果为 PASS**：
> 📋 检测通过！请输入以下指令之一继续：
> - `"文档通过，生成技术方案"` — 直接进入技术方案阶段
> - `"我要修改需求"` — 描述修改内容后重新生成文档

**若检测结果为 FAIL**：
> ❌ 文档存在必须修复的问题，请：
> - 描述修改内容，我来更新文档并重新检测
> - 或输入 `"忽略问题，强制通过"` 跳过检测（不推荐）

---

### 第五步：迭代处理

用户提出修改意见后：
1. 更新 `projects/{currentProject}/01-需求/requirements.md`（同时更新版本号 v1.x）
2. 重新执行检测流程（回到第一步）
3. 输出新版检测报告

**多轮迭代记录**：每次更新在文档顶部更新版本号和修改日志。
