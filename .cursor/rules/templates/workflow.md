# {项目名称} 工作空间流程状态

> 规则来源：`.cursor/rules/00-核心工作流程.mdc`  
> 用途：记录当前流程进度、总体流程列表、恢复点、需加载规则、依赖文件和注意事项。  
> 维护规则：每次生成、修改、检测或编码完成后必须更新本文档。

---

## 1. 当前流程

| 字段 | 内容 |
|------|------|
| 当前阶段 | `{阶段名}` |
| 当前步骤 | `{步骤名}` |
| 当前步骤状态 | `pending` |
| 当前恢复点 | `{从哪里继续}` |
| 下一步默认动作 | `{下一步动作}` |

状态枚举：

| 状态 | 含义 |
|------|------|
| `pending` | 未开始 |
| `in_progress` | 执行中 |
| `waiting_review` | 待用户审核或确认 |
| `done` | 已完成 |
| `blocked` | 被缺失信息、冲突或外部条件阻塞 |
| `skipped` | 用户明确跳过 |

---

## 2. 总体流程列表

> 说明：本表用于记录“从当前版本目标出发”整体还需要做哪些事，以及哪些已经完成、哪些尚未完成。  
> 若出现新增需求、局部返工或二次调整，即使某个阶段曾经完成过，只要受本轮变更影响且尚未同步到位，就应重新标记为 `pending` 或 `in_progress`。

| 序号 | 流程项 | 目标产物 / 目标动作 | 当前状态 | 说明 |
|------|--------|----------------------|----------|------|
| 0 | 变更校准 | `{change-plan.md / 本轮变更计划}` | `pending` | `{新增需求、返工或局部调整时，先登记影响范围}` |
| 1 | 需求分析 | `{需求清单 / 需求摘要}` | `pending` | `{需求是否已沉淀成列表}` |
| 2 | PRD / 需求文档 | `{requirements.md}` | `pending` | `{PRD 是否已通过}` |
| 3 | 产品文档 | `{产品文档路径}` | `pending` | `{页面、路由、状态、数据结构是否已同步}` |
| 4 | Figma 设计更新 | `{Figma 文件 / 页面}` | `pending` | `{设计稿是否已更新到当前版本}` |
| 5 | Figma 设计计划文档 | `{figma-design-plan.md}` | `pending` | `{仓库内设计计划是否存在并已同步}` |
| 6 | 技术方案同步 | `{tech-plan.md}` | `pending` | `{总体架构、模块边界与接口是否已同步}` |
| 7 | 前端技术文档同步 | `{flutter-tech.md 或前端技术文档}` | `pending` | `{前端实现口径是否已同步}` |
| 8 | 后端技术文档同步 | `{kotlin-tech.md 或后端技术文档}` | `pending` | `{接口、数据结构、同步策略是否已同步}` |
| 9 | 总体开发计划同步 | `{flutter-dev-plan.md 或开发计划}` | `pending` | `{任务顺序、恢复点、验收路径是否已同步}` |
| 10 | 模块开发文档同步 | `{开发计划/*.md}` | `pending` | `{模块级计划是否已补齐}` |
| 11 | 编码实现 | `{代码工程路径}` | `pending` | `{属于阻塞式代码开发阶段；仅在前置文档同步后进入正式实现}` |
| 12 | 后端代码开发 | `{后端工程路径}` | `pending` | `{属于阻塞式代码开发阶段；进入后默认在当前编码步骤内持续修改和调试}` |
| 13 | 前端接入后端接口 | `{前端工程对接真实 API}` | `pending` | `{属于阻塞式代码开发阶段；通常依赖后端接口稳定后持续联调}` |

---

## 3. 当前步骤需加载规则

| 规则文件 | 用途 |
|----------|------|
| `.cursor/rules/00-核心工作流程.mdc` | 总调度、上下文加载、执行契约 |
| `{步骤规则文件}` | 当前步骤执行规则 |

---

## 4. 当前步骤依赖文件

| 文件 | 用途 | 是否必须 |
|------|------|----------|
| `{项目文件路径}` | `{为什么需要读取}` | 是 |

---

## 5. 当前步骤生成文件

| 文件 | 状态 | 说明 |
|------|------|------|
| `{项目文件路径}` | `pending` | `{产物说明}` |

---

## 6. 额外注意事项

- `{需要在当前步骤中特别注意的点}`
- `总体流程列表` 必须优先反映“当前版本真实完成度”，不能只记录历史上曾完成过的阶段。
- `Flutter 编码`、`后端代码开发`、`前端接入后端接口` 都属于阻塞式代码开发阶段；如果用户没有明确说“进入下一步”，默认保持在当前编码步骤内持续修改、调试和回写状态，不自动推进到下一个流程项。

---

## 7. 阻塞项

| 阻塞项 | 影响 | 处理方式 |
|--------|------|----------|
| 无 | 无 | 无 |

---

## 8. 已完成阶段记录

| 阶段 | 产物 | 状态 | 备注 |
|------|------|------|------|
| `{阶段名}` | `{产物路径}` | `pending` | `{备注}` |

---

## 9. 执行记录

| 时间 | 阶段/步骤 | 操作 | 结果 |
|------|-----------|------|------|
| `{YYYY-MM-DD}` | `{阶段/步骤}` | `{执行内容}` | `{结果}` |
