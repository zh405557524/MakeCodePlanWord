---
description: Java 后端通用第三方依赖参考，按依赖类别而不是业务包名组织
globs: "**/build.gradle"
alwaysApply: false
---

# 第三方依赖参考

## 一、依赖分层

第三方依赖分为三类：

1. 基础必选
2. 通用可选
3. 业务专用

规则：

- 先确认用途，再引入依赖
- 不因为参考项目依赖很多就全部照搬
- 版本统一通过 `gradle.properties`、`ext` 变量、Spring Boot 插件或公司级版本约束管理

---

## 二、基础必选

### 2.1 框架

- `org.springframework.boot:spring-boot-starter-web`
- `org.springframework.boot:spring-boot-starter-validation`
- `org.springframework.boot:spring-boot-starter-actuator`
- `org.springframework.boot:spring-boot-starter-security`

### 2.2 序列化

- Spring Boot 默认 Jackson（无需额外引入）
- Java 8+ 时间类型使用 `jackson-datatype-jsr310`（通常由 Spring Boot 管理）

### 2.3 数据库

- `com.baomidou:mybatis-plus-spring-boot3-starter`
- `com.zaxxer:HikariCP`（Spring Boot JDBC 默认连接池）
- 数据库驱动（PostgreSQL：`org.postgresql:postgresql`）

### 2.4 迁移

- `org.flywaydb:flyway-core`（或 Liquibase）

### 2.5 鉴权

- `io.jsonwebtoken:jjwt-api` + runtime impl / jackson
- 或 `com.auth0:java-jwt`

### 2.6 错误处理

- Spring MVC `@RestControllerAdvice`（无需额外依赖）

### 2.7 日志

- Spring Boot 默认 Logback + SLF4J

---

## 三、通用可选

### 3.1 CORS

- Spring Web CORS 配置（无需额外依赖）

### 3.2 调用日志

- `org.springframework.boot:spring-boot-starter-actuator`（配合 access log / tracing）

### 3.3 密码哈希

- Spring Security `PasswordEncoder`
- `org.mindrot:jbcrypt`

### 3.4 缓存

- `org.springframework.boot:spring-boot-starter-data-redis`
- `com.github.ben-manes.caffeine:caffeine`（本地缓存）

### 3.5 限流

- `io.github.resilience4j:resilience4j-ratelimiter`
- 或 Redis Token Bucket

### 3.6 文件 / 对象存储

- `software.amazon.awssdk:s3`（S3 兼容）
- `com.aliyun.oss:aliyun-sdk-oss`

### 3.7 消息队列

- `io.lettuce:lettuce-core`（Redis Streams）
- `org.apache.kafka:kafka-clients`
- `org.springframework.amqp:spring-rabbit`（RabbitMQ）

### 3.8 任务调度

- `org.quartz-scheduler:quartz`
- 或 Spring `@Scheduled`

### 3.9 监控 / APM

- `io.micrometer:micrometer-core`
- `io.sentry:sentry`
- `io.opentelemetry:opentelemetry-sdk`

### 3.10 测试

- `org.springframework.boot:spring-boot-starter-test`
- `org.mockito:mockito-core`
- `org.testcontainers:junit-jupiter`
- `org.testcontainers:postgresql`

---

## 四、业务专用

只有业务明确时才引入：

- 邮件：`org.simplejavamail:simple-java-mail`
- 短信：阿里云 / 腾讯云 SDK
- 支付：Stripe / 微信支付 / 支付宝
- 推送：FCM / 个推 / 极光
- IM：环信 / 融云 SDK
- 富文本：jsoup（HTML 解析）
- 文档：iText / Apache POI
- 图像：thumbnailator / ImageMagick

---

## 五、推荐写法

在 `java-tech.md` 中至少写清：

1. 基础必选依赖与版本
2. 当前项目启用的通用可选依赖
3. 当前项目启用的业务专用依赖
4. 各依赖的初始化时机（哪些随 Spring 容器装配，哪些懒加载）
5. 如有 SDK 涉及隐私收集，必须在 17 阶段隐私政策中同步声明

---

## 六、版本统一

推荐使用 Gradle 插件、`ext` 变量或 `gradle.properties` 统一管理：

```groovy
plugins {
    id 'java'
    id 'org.springframework.boot' version '3.x.x'
    id 'io.spring.dependency-management' version '1.1.x'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

ext {
    mybatisPlusVersion = '3.5.x'
    jjwtVersion = '0.12.x'
}

dependencies {
    implementation "com.baomidou:mybatis-plus-spring-boot3-starter:${mybatisPlusVersion}"
    implementation "io.jsonwebtoken:jjwt-api:${jjwtVersion}"
    runtimeOnly "io.jsonwebtoken:jjwt-impl:${jjwtVersion}"
    runtimeOnly "io.jsonwebtoken:jjwt-jackson:${jjwtVersion}"
}
```

---

## 七、依赖审计

- 每次升级核心依赖（Spring Boot / MyBatis-Plus / Java / JVM）必须先在 `change-plan.md` 登记
- 升级后跑全量测试，确保兼容
- 引入新依赖时，必须评估是否带入安全漏洞、许可证风险
