---
description: Flutter UI 处理规则，提供对稿流程、适配要求、状态处理与资源使用约束
globs: "**/lib/**/*.dart"
alwaysApply: false
---

# UI 处理任务

## 一、设计输入来源

优先级：

1. Figma 真源
2. `figma-design-plan.md`
3. PRD / 产品文档
4. 参考图
5. 现有页面风格

不要只凭单张截图猜交互。

---

## 二、实现顺序

1. 拆页面主区块
2. 判断组件复用
3. 建立页面状态
4. 落主题与样式
5. 补齐交互反馈

---

## 三、组件复用优先级

1. `lib/widgets/`
2. `lib/components/`
3. 当前模块 `widgets/`
4. 新建局部组件

---

## 四、适配规则

若工程启用 `flutter_screenutil`，则统一使用：

- `.w`
- `.h`
- `.sp`
- `.r`

不要在启用适配方案后继续大量使用裸尺寸。

---

## 五、主题规则

- 优先使用 `theme.dart`
- 重复使用的颜色、间距、字号不要散落成魔法数字
- 若页面形成稳定样式令牌，应抽回主题或工具层

---

## 六、状态完整性

UI 实现默认至少考虑：

- `loading`
- `empty`
- `error`
- `disabled`
- `submitting`
- `not_found`

不要只做 happy path。

---

## 七、资源使用

- 资源路径优先通过 `utils/assets.dart` 统一索引
- 不在业务代码中大量硬编码资源路径
- 复杂图形优先资源化或组件化

---

## 八、View 与 Controller 协作

UI 改动时同步检查 Controller：

- 是否需要新增状态字段
- 是否需要新增交互方法
- 是否需要补空状态/错误态
- 是否需要补路由返回逻辑

不要只在 View 层打补丁。

---

## 九、对稿清单

- 背景层次
- 标题与正文层级
- 区块顺序
- 留白与间距
- 卡片圆角、边框、阴影
- 主次按钮层级
- 空状态/错误态/加载态

---

## 十、完成后的检查

- 页面区块齐全
- 组件复用合理
- 适配方式一致
- 状态完整
- 与 Controller 联动正确
- 无明显溢出
- `flutter analyze` 通过
