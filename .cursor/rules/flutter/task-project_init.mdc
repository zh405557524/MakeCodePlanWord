---
description: Flutter 新项目初始化 - 目录骨架、依赖、main/global/routes 起点
globs: "**/pubspec.yaml,**/lib/main.dart"
alwaysApply: false
---

# Flutter 项目初始化任务

## 适用场景

- 从零创建与本工作流配套的 Flutter 工程
- 或把现有工程整理为「apis / pages / routes / services」分层

## 依赖基线（可按项目调整版本号）

在 `pubspec.yaml` 中通常包含：

- `get`：状态管理与路由辅助（与规则中 GetX 约定一致）
- `dio`：HTTP
- `flutter_lints`：静态检查

按需增加：`go_router`、`shared_preferences`、`intl` 等。

## 初始化检查清单

1. **目录**：按 `08-项目结构目录.mdc` 创建 `lib/` 下空目录与占位 `index.dart`。
2. **入口**：`main.dart` 中 `WidgetsFlutterBinding.ensureInitialized()`（若需异步初始化）。
3. **全局**：`global.dart` 中注册 `GetMaterialApp` / 路由、主题、`initialRoute` 或 `router`。
4. **主题**：`theme.dart` 中 `ThemeData` 与暗色占位（若需要）。
5. **HTTP**：`services/http.dart` 中 Dio 基地址、拦截器（Token、日志）、统一错误解析。
6. **路由**：`routes/name.dart` 定义 `RouteName` 常量；`routes/router.dart` 注册 `GoRoute` 列表（与项目选型一致）。

## 禁止

- 在 `main.dart` 内堆叠大量业务逻辑；仅保留启动与注入。

## 完成后

- 确保 `flutter analyze` 无 error；可运行空白首页或 Shell 页。
