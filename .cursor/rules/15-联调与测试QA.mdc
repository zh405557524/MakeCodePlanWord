---
description: 联调与测试 QA 阶段 - 前后端编码完成后，统一联调、覆盖测试矩阵、登记缺陷、出具验收报告
globs:
alwaysApply: false
---

# 联调与测试 QA 规则

## 通用执行契约

执行本规则前，必须先完成核心规则中的工作空间定位：

- 读取 `projects/{currentProject}/workflow.md`；若不存在，先按 `.cursor/rules/templates/workflow.md` 创建。
- 读取本规则，以及 `workflow.md` 中“当前步骤依赖文件”列出的材料。
- 执行前先形成本轮联调与测试计划，再生成或更新 `qa-plan.md`。
- 执行后更新 `workflow.md` 的当前阶段、状态、恢复点、依赖文件、生成文件、注意事项和执行记录。
- 若本轮是二次调整或命中变更管控关键词，先读取或创建 `projects/{currentProject}/change-plan.md`，默认按 `small_change` 限定影响范围。

## 前置条件

执行本规则前，必须确认：

1. 13 Flutter 编码阶段已达成阶段性闭合（Alpha 或更高）
2. 14 Java 编码阶段已达成阶段性闭合（Alpha 或更高）
3. 前后端基础链路已可点通（登录、主流程、关键接口）
4. `projects/{currentProject}/change-plan.md` 顶部「进行中变更」区块已清空或所有条目已明确 defer

## 目标

把前端与后端从「分别完成」推进到「整体可用、可发布」状态。给出可执行的测试矩阵、明确通过判据、闭环缺陷。

QA 阶段是 **串行强门禁**：未达验收不允许进 16 构建打包。

## 输出路径

- `projects/{currentProject}/05-联调与测试/qa-plan.md`
- 缺陷登记文件：`projects/{currentProject}/05-联调与测试/defects/{defect-id}.md`（可选，缺陷较少时直接登记在 `qa-plan.md` 缺陷表中）

## 文档要求

`qa-plan.md` 至少包含：

1. 测试目标与版本范围（对应 `change-log.md` 的版本号区间）
2. 测试环境（dev / staging / 真机型号 / OS 版本 / 后端环境）
3. 联调矩阵

   | 模块 | 前端模块 | 后端接口 | 是否打通 | 备注 |
   |------|----------|----------|----------|------|

4. 测试用例清单
   - 单元测试覆盖（Flutter `flutter test` + Java `./mvnw test`）
   - 集成测试（接口契约、数据库写入读取一致性）
   - 端到端（E2E）用例（主路径 + 关键状态切换 + 异常恢复）
   - UI 验收（对照 `figma-design-plan.md`，至少抽检 30% 关键页面）
5. 性能与可用性
   - 关键页面首屏时长基线
   - 接口 P95 响应时长基线
   - 离线 / 弱网 / 后台恢复行为
6. 兼容性矩阵
   - Android：最低支持 / 中等机型 / 高端机型
   - iOS：最低支持 / 中端 / 高端
   - 屏幕：小屏 / 大屏 / 平板（如支持）
7. 安全检查
   - 鉴权失败路径
   - 越权访问
   - 敏感数据日志
8. 缺陷登记表

   | 缺陷 ID | 标题 | 严重度 | 触发版本 | 复现步骤 | 期望行为 | 当前状态 | 关联 CHG-id |
   |---------|------|--------|----------|----------|----------|----------|--------------|

9. 验收判据
   - P0/P1 缺陷必须全部 `fixed` 且回归通过
   - P2 缺陷可在「待修复清单」中明确接受，进入下个版本
   - 联调矩阵全部「是」
10. 测试报告（终态）
    - 通过率、缺陷分布、关键风险

## 缺陷严重度定义

- **P0**：阻塞核心功能、数据损坏、崩溃、安全漏洞 → 必须修复
- **P1**：主路径功能异常、明显 UI 错误、影响主要用户 → 必须修复
- **P2**：边缘场景异常、轻微 UI 偏差 → 可延期但必须登记
- **P3**：建议优化、不影响功能 → 可延期到后续迭代

## 缺陷修复回流

- 修复一个 P0/P1 缺陷视为变更入口，必须先在 `change-plan.md` 中追加变更条目，再修代码
- 修复后回归通过，把条目归档到 `change-log.md`
- 缺陷登记表中的「关联 CHG-id」必须填实

## 推进规则

- 测试报告生成后必须暂停，等待用户确认验收
- 用户确认通过后，进入 16 构建与打包阶段
- 若 P0/P1 缺陷未全部修复，禁止进入 16 阶段；必须回到对应编码阶段（13 / 14）
