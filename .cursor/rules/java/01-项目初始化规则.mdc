---
description: Java 后端项目初始化规则，提供通用工程骨架、依赖基线与可直接复用的模板代码（Java 21 + Spring Boot + MyBatis-Plus + PostgreSQL + Redis）
globs: "**/build.gradle,**/settings.gradle,**/src/main/java/**/Application.java"
alwaysApply: false
---

# 项目初始化规则

> 本规则用于创建一个新的 Java 后端工程时，快速建立可持续的 AI 编码基线。  
> 默认技术栈：Java 21 + Spring Boot 3.x + Gradle + MyBatis-Plus + PostgreSQL + Redis + JWT + Logback。

> 项目级覆盖（剧说）：后端工程固定为 `/Users/soul/code/lxwx/lxwx_service/lxwxservice`，包名固定为 `com.lxwx.narrateservice`，构建工具固定使用 Gradle / Gradle Wrapper；不要回退到 Maven，也不要按旧 Kotlin 项目记忆生成后端代码。启动入口使用 `ServerApplication.java`，目录结构按 `common/auth/user/product/order/payment/entitlement/app/admin` 分层。

---

## 一、适用场景

- 新建 Java 后端项目
- 把现有工程整理成统一骨架
- 需要快速建立 `Application/config/controller/service/repository/entity/dto/vo/common` 基础设施

---

## 二、初始化目标

初始化阶段至少要完成：

1. 建立标准 Gradle 目录结构
2. 建立 `{Project}Application.java`
3. 建立 `config/`（Redis / Jackson / MyBatis-Plus / Security 配置）
4. 建立 `controller/`
5. 建立 `service/`
6. 建立 `repository/`
7. 建立 `entity/`、`dto/`、`vo/` 或项目文档指定的领域目录
8. 建立 `common/`
9. 建立 `db/migration/`（迁移脚本目录）
10. 产出最小可运行健康检查接口

---

## 三、推荐目录骨架

```text
src/main/java/com/{company}/{project}/
├── {Project}Application.java
├── config/
│   ├── RedisConfig.java
│   ├── JacksonConfig.java
│   ├── MybatisPlusConfig.java
│   └── SecurityConfig.java
├── controller/
│   ├── HealthController.java
│   └── AuthController.java
├── service/
│   ├── AuthService.java
│   └── impl/
├── mapper/
│   └── UserMapper.java
├── domain/
│   ├── dto/
│   ├── vo/
│   └── entity/
│       └── UserEntity.java
└── common/
    ├── ApiResponse.java
    ├── ErrorCode.java
    ├── BusinessException.java
    └── GlobalExceptionHandler.java
src/main/resources/
├── application.properties
├── mapper/
└── db/migration/
    └── V1__init.sql
```

---

## 四、推荐依赖基线（build.gradle）

```groovy
plugins {
    id 'java'
    id 'org.springframework.boot' version '3.x.x'
    id 'io.spring.dependency-management' version '1.1.x'
}

group = 'com.example'
version = '0.1.0'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

repositories {
    mavenCentral()
}

ext {
    mybatisPlusVersion = '3.5.x'
    jjwtVersion = '0.12.x'
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-validation'
    implementation 'org.springframework.boot:spring-boot-starter-security'
    implementation 'org.springframework.boot:spring-boot-starter-data-redis'
    implementation 'org.springframework.boot:spring-boot-starter-actuator'
    implementation "com.baomidou:mybatis-plus-spring-boot3-starter:${mybatisPlusVersion}"
    implementation 'org.flywaydb:flyway-core'
    implementation 'org.mindrot:jbcrypt:0.4'
    implementation "io.jsonwebtoken:jjwt-api:${jjwtVersion}"
    runtimeOnly 'org.postgresql:postgresql'
    runtimeOnly "io.jsonwebtoken:jjwt-impl:${jjwtVersion}"
    runtimeOnly "io.jsonwebtoken:jjwt-jackson:${jjwtVersion}"
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

tasks.named('test') {
    useJUnitPlatform()
}
```

---

## 五、模板代码

### 5.1 `{Project}Application.java`

```java
package com.example.project;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ProjectApplication {
    public static void main(String[] args) {
        SpringApplication.run(ProjectApplication.class, args);
    }
}
```

### 5.2 `controller/HealthController.java`

```java
package com.example.project.controller;

import com.example.project.common.ApiResponse;
import java.util.Map;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/health")
public class HealthController {
    @GetMapping
    public ApiResponse<Map<String, String>> health() {
        return ApiResponse.success(Map.of("status", "up"));
    }
}
```

### 5.3 `mapper/UserMapper.java`

```java
package com.example.project.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.example.project.domain.entity.UserEntity;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface UserMapper extends BaseMapper<UserEntity> {
}
```

### 5.4 `config/RedisConfig.java`

```java
package com.example.project.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.data.redis.repository.configuration.EnableRedisRepositories;

@Configuration
@EnableRedisRepositories
public class RedisConfig {
}
```

### 5.5 `common/GlobalExceptionHandler.java`

```java
package com.example.project.common;

import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(Exception.class)
    public ApiResponse<Void> handle(Exception ex) {
        return ApiResponse.error(5000, ex.getMessage() == null ? "Internal error" : ex.getMessage());
    }
}
```

### 5.6 `common/ApiResponse.java`

```java
package com.example.project.common;

public record ApiResponse<T>(int code, String message, T data) {
    public static <T> ApiResponse<T> success(T data) {
        return new ApiResponse<>(0, "success", data);
    }

    public static <T> ApiResponse<T> error(int code, String message) {
        return new ApiResponse<>(code, message, null);
    }
}
```

### 5.7 `resources/application.properties`

```properties
server.port=8080
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/app_db}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
mybatis-plus.configuration.map-underscore-to-camel-case=true
spring.flyway.locations=classpath:db/migration
```

---

## 六、强约束

- 包名按 `com.{company}.{project}` 组织，所有源码放该包内
- `{Project}Application.java` 只负责 Spring Boot 启动
- `config/` 只负责框架接入，不写业务
- 业务请求只经过 `controller → service → mapper` 三层，不允许跨层
- 敏感配置（数据库密码、JWT 密钥）只走环境变量注入
- 不在初始化阶段引入业务 SDK、业务路由、业务实体

---

## 七、完成后的检查清单

- [ ] 项目目录结构完整
- [ ] `{Project}Application.java` 可启动
- [ ] `/health` 接口返回 200 与统一 `ApiResponse`
- [ ] 数据库连接成功，Flyway 迁移脚本可执行
- [ ] Redis 连接成功且关键缓存键可读写
- [ ] JWT 配置已就绪（可在测试环境签发并验证）
- [ ] `./gradlew build` 通过
- [ ] 可在此基础上继续新增业务接口
