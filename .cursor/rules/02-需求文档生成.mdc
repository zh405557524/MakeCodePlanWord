---
description: 需求文档生成阶段。将已确认的需求清单整理为统一格式的 PRD。
globs:
alwaysApply: false
---

# 需求文档生成规则

## 前置条件

生成 PRD 前必须满足：

- 已有 `currentProject`
- 已有经用户确认的需求清单

如果前置条件不满足，先回到需求分析阶段。

## 目标

把需求清单转成一份完整、可读、格式统一、可继续评审的 PRD。

## 基本原则

- 文档要让人能顺着读懂，不要只堆术语。
- 保持结构固定，减少风格漂移。
- 页面、流程、规则要写清楚。
- 如有 UI 素材，优先放在对应页面小节下。
- 避免写死具体业务模板，保持可复用。

## PRD 固定结构

PRD 按以下顺序输出：

1. 产品概述
2. 当前版本范围
3. 整体流程
4. 信息架构 / 页面结构
5. 页面级详细需求
6. 数据与同步
7. 核心业务规则
8. 前后端交互说明
9. 非功能要求
10. 里程碑
11. 待确认项

## 页面级固定模板

每个页面小节统一包含：

- 页面定位
- 参考素材（有素材时直接显示在该页面小节内）
- 页面结构（从上到下）
- 区块说明
- 主要功能
- 交互流程图
- 详细交互说明
- 关键状态 / 异常情况
- 验收标准

## 流程表达要求

- “整体流程”和“页面交互流程”优先使用流程图。
- 流程图使用 Mermaid `flowchart TD`。
- 流程图节点只写通用步骤，不写无关废话。
- 流程图后可补 3-6 行文字说明，但不要替代流程图。

## 页面拆解要求

- 页面说明必须按 UI 从上到下拆解，不要只写抽象功能点。
- 每个区块至少写清位置、内容、作用、交互动作、反馈结果。
- 复杂交互要单独拆出动作链，不能只写“点击后跳转”。

## 页面交互要求

页面交互不要只写“跳转到哪里”，至少写清：

- 用户从哪里进入
- 先看到什么
- 做什么操作
- 系统如何反馈
- 下一步流向哪里

## 输出路径

输出到：

- `projects/{currentProject}/01-需求/requirements.md`

## 推进规则

- PRD 生成后，不要直接进入后续阶段。
- 提示用户进入“需求文档检测”。
