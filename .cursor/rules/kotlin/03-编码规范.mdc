---
description: Kotlin 后端通用编码规范，强调分层、命名、空安全、异常分类、KDoc、日志与错误处理
globs: "**/src/main/kotlin/**/*.kt"
alwaysApply: true
---

# 编码规范

## 一、文件组织

- 使用领域 / 分层目录
- 一个文件一个 public 类（顶层函数与扩展可同文件共存）
- 相同领域的 routes / service / repository / model 必须放在对应 layer 目录里，不允许把多层混在一个文件

---

## 二、命名规范

- 包名：`com.{company}.{project}.{layer}`，全小写
- 类、接口、对象、枚举：`PascalCase`
- 函数、变量、属性：`camelCase`
- 常量（const val）：`UPPER_SNAKE_CASE`
- 测试类：`{被测类}Test`
- DTO 后缀：`Request` / `Response`，避免使用 `DTO`、`VO`、`Bean` 等含糊后缀

---

## 三、空安全

- 默认拒绝 `!!`，必须用 `?.let { }` / `?: error("...")` / `requireNotNull(...)` 替代
- 公共 API 中尽量减少可空类型，可空必须在 KDoc 中说明含义
- `lateinit var` 仅用于注入或测试，不在业务字段中滥用

---

## 四、异常分类

业务异常分为三类，禁止直接 `throw Exception("...")`：

1. `IllegalArgumentException` / `BadRequestException`：参数校验失败 → 1001
2. `UnauthorizedException`：鉴权失败 → 1002 / 1003
3. `NotFoundException`：资源不存在 → 1004
4. 自定义 `BusinessException(val code: Int, override val message: String)`：业务规则失败

所有异常由 StatusPages 统一拦截，转成 `ApiResponse`。Service 层禁止直接调用 `call.respond(...)`。

---

## 五、KDoc 注释

- 所有 public 类、public 函数必须写 KDoc
- KDoc 使用中文，描述「负责什么 / 为什么这样做」，不重复代码字面意思
- 至少描述：参数含义、返回含义、可能抛出的异常、并发 / 事务约束

示例：

```kotlin
/**
 * 根据邮箱查找用户。
 *
 * @param email 用户邮箱，调用方保证已 trim 并小写化
 * @return 命中用户实体，未命中返回 null
 * @throws IllegalArgumentException 当 email 为空字符串
 */
fun findByEmail(email: String): User?
```

---

## 六、状态与并发

- Service 默认无状态；如必须持有状态，需通过 DI 单例 + 线程安全数据结构
- 数据库访问统一通过 `transaction { ... }` 包裹
- 协程使用 `Dispatchers.IO` 处理 IO 密集操作，不直接在主线程跑同步数据库调用

---

## 七、错误处理

- 异步调用统一处理异常，不允许吞掉关键失败
- 关键错误必须进入日志（warn / error 级别）
- 不允许在 Routes 层 catch 后返回 `try { } catch { return 500 }`，统一交给 StatusPages

---

## 八、日志规范

- 使用 SLF4J + Logback
- 每个类用 `private val logger = LoggerFactory.getLogger(javaClass)` 获取 logger
- 关键业务操作（登录、数据写入、外部调用）必须 `logger.info(...)`
- 异常必须 `logger.error("context", throwable)` 带上下文
- 不在日志中输出密码、Token、身份证号等敏感信息

---

## 九、配置与常量

- 敏感配置走 `application.conf` + 环境变量
- 业务常量集中到 `utils/Constants.kt` 或对应领域的 `companion object`
- 不在业务代码中硬编码 baseUrl、密钥、超时时间

---

## 十、验证要求

- 改动后至少通过 `./gradlew build`
- 关键链路改动后补单元测试或集成测试
- 文件、命名、目录、依赖关系必须保持一致
