---
description: 需求分析阶段 - 引导用户提炼需求，建立项目目录，输出结构化需求清单
globs: 
alwaysApply: false
---

# 需求分析规则

## 执行步骤

### 第一步：确认项目名称

**必须首先询问并确认项目名称**，后续所有文档都将存放在此目录下：

> 请告诉我项目名称（例如：电商App、社交平台、企业管理系统）？
> 若未指定，将使用今天日期命名（如：20260331-未命名项目）

记录项目名称为 `currentProject`，后续所有文档路径均使用：
```
projects/{currentProject}/
```

---

### 第二步：引导需求提炼

按以下维度逐步引导用户描述需求，每个维度都需要得到明确回答后才能继续：

#### 2.1 项目背景
- 这个项目要解决什么问题？
- 目标用户是谁？
- 与同类产品相比有什么不同？

#### 2.2 核心功能
- 列举最重要的 3-5 个核心功能
- 每个功能的具体表现是什么？
- 用户如何使用这些功能？

#### 2.3 技术约束
- 前端是否使用 Flutter？目标平台（iOS/Android/Web）？
- 后端是否使用 Kotlin？框架偏好（Ktor/Spring Boot）？
- 数据存储方案？（本地/云端/两者都有）
- 是否有第三方服务集成需求？

#### 2.4 非功能性需求
- 性能要求（响应时间、并发量）？
- 安全要求（用户鉴权方式）？
- 是否需要离线支持？

---

### 第三步：输出结构化需求清单

将收集到的信息整理为以下格式，**不要直接生成 PRD**，先输出清单供用户确认：

```markdown
## 需求清单 - {currentProject}

### 项目概述
- 项目名称：
- 解决问题：
- 目标用户：

### 核心功能列表
1. 功能名称 | 描述 | 优先级（高/中/低）
2. ...

### 技术约束
- 前端：Flutter（目标平台：）
- 后端：Kotlin（框架：）
- 数据库：
- 第三方集成：

### 非功能性需求
- 性能：
- 安全：
- 离线支持：是/否
```

---

### 第四步：确认并推进

输出需求清单后，**等待用户确认**：
- 用户说"确认"、"没问题"、"继续" → 提示用户进入下一阶段（需求文档生成）
- 用户提出修改 → 按要求更新需求清单，重新输出

**推进提示语**（用户确认后输出）：
> 需求清单已确认。下一步：输入"生成需求文档"将需求清单转为完整的 PRD 文档。
