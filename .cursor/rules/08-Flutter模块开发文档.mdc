---
description: Flutter 模块开发文档阶段 - 基于总体开发计划与参考工程为单模块生成可直接驱动 AI 编码的文档
globs:
alwaysApply: false
---

# Flutter 模块开发文档规则

## 前置条件

执行本规则前，必须确认：
1. `projects/{currentProject}/03-前端/flutter-dev-plan.md` 已存在
2. 用户已确认总体开发计划可继续
3. `projects/{currentProject}/03-前端/flutter-tech.md` 已存在
4. `projects/{currentProject}/01-需求/figma-design-plan.md` 已存在

## 参考工程

当前模块开发文档默认参考：

- `G:\code\soul\Solfeggio`

目标不是照搬其业务，而是复用其：

- 目录组织
- 页面拆分
- Controller 写法
- Service / Store 关系
- 路由与初始化方式
- 通用组件风格

## 目标

为单个模块生成独立开发文档，保证：

- 一个模块一个文件
- 修改单个模块文档时，不影响总体开发计划
- 文档足够细致，可直接驱动 AI 编码
- 优先按 `01-需求/figma-design-plan.md` + `01-需求/references/images/` 输出视觉与状态要求

## 输出路径

- `projects/{currentProject}/03-前端/开发计划/{module-name}.md`

## 文档要求

每个模块开发文档至少包含：

1. 功能定位与边界
2. 入口、路由与页面文件
3. 依赖前置
4. 视觉参考与设计稿分析
5. 涉及模块与类
6. 业务内容与数据流
7. 现有工程参考点
8. 建议实现步骤
9. 详细编码任务清单
10. 验收标准
11. 编码注意事项

## 视觉分析要求

- 默认先读取 `01-需求/figma-design-plan.md`，再读取 `01-需求/references/images/`
- 写清参考了哪些图片
- 写清该模块对应的页面、状态、弹层、组件要求来自 `figma-design-plan.md` 的哪些部分
- 从图片中提取页面从上到下区块、主按钮、弹框、状态差异
- 若图片与 PRD 不完全一致，写明“当前图中表现”与“当前 PRD 目标版本”

## 推进规则

- 生成模块文档时，同步更新总体开发计划中的模块状态或记录
- 模块文档生成完成后，等待用户决定是否开始编码或继续处理下一个模块
