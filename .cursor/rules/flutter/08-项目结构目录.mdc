---
description: Flutter 项目 lib/ 与根目录通用结构索引，供生成技术文档与编码时快速定位
globs: "**/lib/**/*.dart"
alwaysApply: false
---

# Flutter 项目结构目录

## 根目录（常见）

- `pubspec.yaml`：依赖与资源
- `lib/`：Dart 源码主目录
- `assets/`：图片、字体等（若使用）
- `test/` / `integration_test/`：测试

## `lib/` 标准骨架（与本仓库 `05-前端Flutter技术文档.mdc` 模板一致）

```
lib/
├── apis/                 # 接口封装，按模块或聚合 index
├── components/           # 跨页复用中型组件（可选）
├── enums/
├── models/
│   ├── params/           # 请求参数模型
│   ├── responses/        # 响应模型
│   └── index.dart
├── pages/
│   ├── main/             # 壳、Tab、主导航
│   └── {feature_name}/   # 功能页，每功能独立子目录
├── plugins/              # 第三方插件封装入口（可选）
├── routes/
│   ├── index.dart
│   ├── router.dart       # GoRouter / 路由表注册
│   ├── name.dart         # 路由名常量 RouteName
│   └── observers.dart
├── services/
│   ├── http.dart         # Dio 等
│   ├── storage.dart
│   ├── event.dart
│   └── index.dart
├── store/                # 全局状态（GetxService 等，可选）
├── utils/
├── widgets/              # 通用小组件
├── global.dart           # 全局绑定、初始化
├── main.dart
└── theme.dart            # ThemeData、色板、TextTheme
```

## 约定

- 各目录宜提供 `index.dart` 统一导出对外 API，避免深层相对路径泛滥。
- 业务页面只放在 `pages/{feature_name}/`，勿在根目录堆叠散乱 `xxx_page.dart`。
- 新增功能时目录名使用 **snake_case**（与 Dart 文件命名习惯一致）。
