---
description: Java 模块开发文档阶段 - 基于总体开发计划为单后端模块生成可直接驱动 AI 编码的文档
globs:
alwaysApply: false
---

# Java 模块开发文档规则

## 通用执行契约

执行本规则前，必须先完成核心规则中的工作空间定位：

- 读取 `projects/{currentProject}/workflow.md`；若不存在，先按 `.cursor/rules/templates/workflow.md` 创建。
- 读取本规则，以及 `workflow.md` 中“当前步骤依赖文件”列出的材料。
- 执行前先形成本轮模块开发计划，再生成或更新对应模块开发文档。
- 执行后更新 `workflow.md` 的当前阶段、状态、恢复点、依赖文件、生成文件、注意事项和执行记录。
- 若本轮是二次调整或命中变更管控关键词，先读取或创建 `projects/{currentProject}/change-plan.md`，默认按 `small_change` 限定影响范围。

## 前置条件

执行本规则前，必须确认：

1. `projects/{currentProject}/04-后端/java-dev-plan.md` 已存在
2. 用户已确认 Java 总体开发计划可继续
3. `projects/{currentProject}/04-后端/java-tech.md` 已存在

## 目标

为单个后端模块生成独立开发文档，保证：

- 一个模块一个文件
- 修改单个模块文档时，不影响 Java 总体开发计划
- 文档足够细致，可直接驱动 AI 编码
- 接口契约（路径、请求体、响应体、错误码）必须与前端预期一致

## 输出路径

- `projects/{currentProject}/04-后端/开发计划/{module-name}.md`

## 文档要求

每个模块开发文档至少包含：

1. 功能定位与边界
2. 涉及的 Controller / Service / Mapper / Entity 清单
3. 接口契约
   - 路径、方法、请求体、响应体、错误码、鉴权要求
   - 与前端模块开发文档的引用关系（如 `03-前端/开发计划/{对应模块}.md`）
4. 数据表与迁移
   - 涉及表名、字段、索引
   - 是否新增 / 修改迁移脚本
5. 业务规则与状态流转
6. 异常处理与日志要求
7. 依赖前置（其他模块、外部 SDK、消息队列等）
8. 建议实现步骤
9. 详细编码任务清单（任务 ID 与 `java-dev-plan.md` 对齐）
10. 测试要求
    - 单元测试覆盖范围
    - 集成测试用例
    - 契约测试（与前端约定）
11. 验收标准
12. 编码注意事项

## 接口契约要求

- 接口契约必须从 `java-tech.md` 与 `tech-plan.md` 中复制并细化，不允许在模块文档中独立发明字段。
- 若需要新增字段，必须先在 `change-plan.md` 中登记变更条目，并同步更新 `java-tech.md`。
- 模块文档的接口契约段必须能独立支撑前端联调，不依赖外部文档。

## 测试要求

- 每个核心接口至少 1 个集成测试用例（含正常路径 + 至少 1 个错误码路径）
- Service 关键业务方法必须有单元测试
- Mapper 层若有自定义查询，必须有 SQL 行为验证

## 推进规则

- 生成模块文档时，同步更新总体开发计划中的模块状态或记录
- 模块文档生成完成后，等待用户决定是否开始编码或继续处理下一个模块
- 编码阶段触发后，由 `14-Java编码索引.mdc` 接管
