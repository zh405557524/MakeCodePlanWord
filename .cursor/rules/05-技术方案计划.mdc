---
description: 技术方案计划阶段 - 基于已确认的 PRD 与 Figma 设计计划生成完整技术方案，拆分前端 Flutter 和后端 Kotlin 任务
globs:
alwaysApply: false
---

# 技术方案计划规则

## 通用执行契约

执行本规则前，必须先完成核心规则中的工作空间定位：

- 读取 `projects/{currentProject}/workflow.md`；若不存在，先按 `.cursor/rules/templates/workflow.md` 创建。
- 读取本规则，以及 `workflow.md` 中“当前步骤依赖文件”列出的材料。
- 执行前先形成本轮技术方案计划，再生成或更新技术方案文档。
- 执行后更新 `workflow.md` 的当前阶段、状态、恢复点、依赖文件、生成文件、注意事项和执行记录。
- 若本轮是二次调整或命中变更管控关键词，先读取或创建 `projects/{currentProject}/change-plan.md`，默认按 `small_change` 限定影响范围。

## 前置条件

执行本规则前，必须确认：

1. `projects/{currentProject}/01-需求/requirements.md` 已存在且已通过检测
2. `projects/{currentProject}/01-需求/figma-design-plan.md` 已存在
3. 用户已明确说"文档通过，生成技术方案"或类似确认指令

---

## 执行步骤

### 第一步：深度阅读 PRD

读取 `projects/{currentProject}/01-需求/requirements.md` 与 `projects/{currentProject}/01-需求/figma-design-plan.md`，重点提取：
- 所有功能模块及其优先级
- 页面、状态、组件与交互的设计约束
- 技术约束（Flutter 版本/平台、Kotlin 框架）
- 性能和安全要求
- 里程碑规划

---

### 第二步：设计整体架构

输出系统整体架构说明，包括：

**架构模式**（根据需求选择）：
- 前端：Flutter + GetX / Riverpod / BLoC
- 后端：Kotlin + Ktor RESTful API / Spring Boot
- 数据库：根据需求选择（SQLite/PostgreSQL/MySQL/Firebase）
- 通信协议：HTTP REST / WebSocket（如需实时功能）

---

### 第三步：生成技术方案文档

输出到：`projects/{currentProject}/02-技术方案/tech-plan.md`

**文档模板**：

```markdown
# {项目名称} 技术方案计划

> 版本：v1.0  
> 创建日期：{今天日期}  
> 关联需求文档：../01-需求/requirements.md  
> 关联设计计划：../01-需求/figma-design-plan.md

---

## 1. 系统架构概述

### 1.1 整体架构
（描述前后端分离架构、通信方式）

### 1.2 技术栈选型
| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|---------|
| 前端 | Flutter | | |
| 状态管理 | | | |
| 后端 | Kotlin + | | |
| 数据库 | | | |
| 部署 | | | |

---

## 2. 数据库设计

### 2.1 核心数据表
（列出主要实体及其关系）

### 2.2 ER 图（文字描述）
（描述实体关系）

---

## 3. API 接口规划

### 3.1 接口列表

| 模块 | 方法 | 路径 | 描述 |
|------|------|------|------|
| 用户 | POST | /api/auth/login | 用户登录 |
| 用户 | POST | /api/auth/register | 用户注册 |
| ... | ... | ... | ... |

### 3.2 接口规范
- 统一返回格式：`{ code, message, data }`
- 鉴权方式：JWT Bearer Token
- 错误码规范：（列出主要错误码）

---

## 4. 前端模块划分（Flutter）

### 4.1 页面路由规划
| 路由 | 页面 | 说明 |
|------|------|------|
| /login | 登录页 | |
| /home | 首页 | |
| ... | ... | ... |

### 4.2 核心模块列表
| 模块 | 功能描述 | 优先级 |
|------|---------|--------|
| | | |

### 4.3 状态管理方案
（描述全局状态、局部状态的管理策略）

### 4.4 设计约束引用
（说明前端页面、状态、组件与交互稿要求如何对齐 `figma-design-plan.md`）

---

## 5. 后端模块划分（Kotlin）

### 5.1 项目结构
```
src/
├── controller/    # 路由处理
├── service/       # 业务逻辑
├── repository/    # 数据访问
├── model/         # 数据模型
└── util/          # 工具类
```

### 5.2 核心模块列表
| 模块 | 功能描述 | 优先级 |
|------|---------|--------|
| | | |

---

## 6. 开发任务拆分

### 6.1 前端任务（Flutter）
| 任务 | 描述 | 预估工作量 | 优先级 |
|------|------|----------|--------|
| T-F001 | | | 高 |
| T-F002 | | | 中 |

> 前端任务拆分时，必须覆盖 `figma-design-plan.md` 中的页面、关键状态、组件与交互约束。
> 任务 ID（T-F00x / T-B00x）必须在后续 06 工时评估阶段一一对应，不可遗漏或并条。

### 6.2 后端任务（Kotlin）
| 任务 | 描述 | 预估工作量 | 优先级 |
|------|------|----------|--------|
| T-B001 | | | 高 |
| T-B002 | | | 中 |

---

## 7. 开发阶段规划

| 阶段 | 前端任务 | 后端任务 | 里程碑 |
|------|---------|---------|--------|
| 第一阶段（MVP） | | | |
| 第二阶段 | | | |
| 第三阶段 | | | |

> 阶段规划只给出顺序与边界，具体工时与日历日期由 06 工作时长评估阶段输出。

---

## 8. 风险与应对

| 风险 | 影响 | 应对方案 |
|------|------|---------|
| | | |
```

---

### 第四步：推进到工时评估阶段

文件写入完成后输出：

> 技术方案已生成：`projects/{currentProject}/02-技术方案/tech-plan.md`
> 
> 下一步默认进入「工作时长评估」，请输入：
> - `"评估工时"` / `"估算开发时间"` — 基于本技术方案做三点估计与里程碑
>
> 工时评估通过后，再进入前端 Flutter 技术文档与后端 Kotlin 技术文档阶段。
