---
description: Java 后端控制器规则，强约束 Controller → Service → Mapper 三层职责分离
globs: "**/src/main/java/**/controller/**/*.java"
alwaysApply: false
---

# 控制器规则

## 一、三层职责

| 层 | 职责 | 禁止 |
|----|------|------|
| Controller | 参数解析、鉴权检查、调用 Service、返回 ApiResponse | 业务逻辑、直接访问数据库、复杂事务 |
| Service | 业务编排、跨 Mapper 调用、事务边界、缓存策略 | 直接读写 HTTP request/response、拼裸 SQL |
| Mapper | 数据访问（MyBatis-Plus / XML SQL） | 业务规则判断、HTTP 上下文 |

---

## 二、Controller 标准模式

```java
@RestController
@RequestMapping("/api/users")
public class UserController {
    private final UserService service;

    public UserController(UserService service) {
        this.service = service;
    }

    @PostMapping
    public ApiResponse<UserResponse> create(@Valid @RequestBody CreateUserRequest req) {
        return ApiResponse.success(service.create(req));
    }

    @GetMapping("/{id}")
    public ApiResponse<UserResponse> detail(@PathVariable Long id) {
        return ApiResponse.success(service.findById(id));
    }
}
```

约定：

- 一个领域一个 `XxxController.java`
- 路径统一前缀 `/api`
- Service 通过构造器注入，不在方法里 `new`

---

## 三、参数解析

- 路径参数：`@PathVariable`
- 查询参数：`@RequestParam`
- 请求体：`@RequestBody`
- 头部：`@RequestHeader`
- 鉴权用户：`SecurityContextHolder.getContext().getAuthentication()`

不允许：

- 在 Controller 中直接读取原始 JSON 字符串后手动解析
- 把多种入参塞到同一个 `Map<String, Object>`

---

## 四、鉴权接入

- 受保护接口通过 Spring Security 规则控制（`SecurityFilterChain`）
- 未受保护接口（注册、登录、健康检查）在 security config 显式 `permitAll`
- 细粒度权限用 `@PreAuthorize`（按需）

---

## 五、错误处理

Controller 不允许 try/catch 业务异常并自行返回 5xx：

- 参数错误 → `throw new IllegalArgumentException(...)`
- 资源不存在 → `throw new NotFoundException(...)`
- 业务规则错误 → `throw new BusinessException(code, message)`

所有异常由 `@RestControllerAdvice` 拦截转 `ApiResponse`。

---

## 六、响应规范

- 统一返回 `ApiResponse<T>`
- 成功：`code = 0`
- 失败：`code` 从错误码规范取
- 不直接返回裸字符串或无统一结构 JSON
- 文件下载、SSE、WebSocket 等特殊情况除外

---

## 七、Service 接入

- Service 单例由 Spring 容器管理（`@Service`）
- Controller 构造器注入 Service
- 不在 Controller 里 `new UserService(...)`，避免手动创建

---

## 八、Mapper 调用

Mapper 永远由 Service 调用，不允许：

- Controller 直接调用 Mapper
- 多个 Service 嵌套调用同一个 Mapper 的写操作而不打事务

---

## 九、事务边界

- 单写操作：在 Service 方法上 `@Transactional`
- 跨表 / 多步写：在 Service 聚合方法上 `@Transactional(rollbackFor = Exception.class)`
- 禁止把事务边界放到 Controller 层

---

## 十、版本化路由

- 默认路径前缀：`/api`
- 如需多版本：`/api/v1`、`/api/v2`
- 不兼容变更必须新增版本，不在原版本上偷偷改

---

## 十一、禁止事项

- 不把业务逻辑写在 Controller 内（包括校验之外的判断）
- 不在 Controller 内拼接 SQL
- 不在 Controller 内做复杂权限判断（下沉到 Service）
- 不在 Controller 内操作数据库连接
