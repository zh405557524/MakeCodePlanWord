---
description: 发布与版本管理阶段 - 审核通过后规划灰度策略、生成用户面向 changelog、明确回滚与热修边界
globs:
alwaysApply: false
---

# 发布与版本管理规则

## 通用执行契约

执行本规则前，必须先完成核心规则中的工作空间定位：

- 读取 `projects/{currentProject}/workflow.md`；若不存在，先按 `.cursor/rules/templates/workflow.md` 创建。
- 读取本规则，以及 `workflow.md` 中“当前步骤依赖文件”列出的材料。
- 执行前先形成本轮发布计划，再生成或更新 `release-plan.md`、`changelog.md`。
- 执行后更新 `workflow.md` 的当前阶段、状态、恢复点、依赖文件、生成文件、注意事项和执行记录。
- 若本轮是二次调整或命中变更管控关键词，先读取或创建 `projects/{currentProject}/change-plan.md`，默认按 `small_change` 限定影响范围。

## 前置条件

执行本规则前，必须确认：

1. 17 应用商店上架阶段已 PASS（所有目标商店审核通过）
2. 已确认本次正式发布的版本号、构建号
3. 已确认本次发布关联的 `change-log.md` 版本号区间

## 目标

把审核通过的版本「以正确的策略放给用户」，并保证：

- 用户面向的更新说明清晰、准确、合规
- 灰度 / 全量发布有明确节奏与回滚预案
- 热修复策略明确，避免绕过本流程直接改线上
- 与下一轮迭代衔接顺畅

## 输出路径

- `projects/{currentProject}/08-发布版本/release-plan.md`（发布计划）
- `projects/{currentProject}/08-发布版本/changelog.md`（**用户面向**的版本日志，与根目录 `change-log.md` 是两份文件）

## `release-plan.md` 固定结构

1. 发布版本信息
   - 版本号 / 构建号
   - 关联 `change-log.md` 版本号区间
   - 目标用户群
   - 计划发布时间窗口
2. 灰度策略

   | 阶段 | 比例 / 范围 | 触发条件 | 监控指标 | 决策窗口 |
   |------|-------------|----------|----------|----------|
   | 内部 | 团队 + 白名单 | 上架通过即开始 | 崩溃率 < X% | 24h |
   | 灰度 1 | 5% 用户 | 内部通过 | 崩溃率、ANR、关键路径成功率 | 48h |
   | 灰度 2 | 20% 用户 | 灰度 1 通过 | 同上 | 48h |
   | 灰度 3 | 50% 用户 | 灰度 2 通过 | 同上 | 24h |
   | 全量 | 100% | 灰度 3 通过 | 同上 | - |

3. 关键监控指标基线
   - 崩溃率上限（如 < 0.3%）
   - ANR 率上限（Android）
   - 启动成功率
   - 关键接口成功率
   - 关键路径完成率
4. 回滚预案
   - 触发条件（崩溃率 > X%、关键路径成功率 < Y%、合规问题）
   - 回滚方式（商店下架旧版？停止灰度？强制升级到下一个修复版？）
   - 回滚责任人与决策链路
5. 热修复策略与边界
   - 允许热修的范围（仅修复 P0/P1 崩溃，禁止借机加新功能）
   - 热修必须经过 `change-plan.md` 登记，按 `feature_modify` 类型走最小改动
   - 热修后必须补一份 v{patch} 的 `changelog.md` 条目
6. 渠道差异
   - 各商店发布时间是否同步
   - 各商店灰度能力差异（AppStore 阶段性发布 / Google Play SSR / 国内商店渠道差异）
7. 公告与通知
   - 应用内公告
   - 推送通知
   - 外部公告（官网、社交媒体、邮件）

## `changelog.md`（用户面向）固定结构

> 注意：本文件与项目根 `change-log.md`（研发面向）是两份不同的文件，前者面向用户、后者面向研发，通过版本号串联。
> 用户面向 `changelog.md` 只展示用户能感知的功能更新，禁止暴露内部 CHG-id 或文件路径。

```markdown
# {项目名称} 版本日志

## v{X.Y.Z} - YYYY-MM-DD

### 新增
- 用户能感知的新功能 A
- 用户能感知的新功能 B

### 优化
- 用户能感知的体验改进
- 性能 / 稳定性提升

### 修复
- 修复了 XXX 问题（不暴露内部技术细节）

### 已知问题
- （如有）描述对用户的影响与临时方案

---

## v{X.Y.Z-1} - YYYY-MM-DD

（历史版本）
```

## 与研发 change-log.md 的同步规则

生成 `changelog.md`（用户面向）时：

1. 从 `change-log.md`（研发面向）按版本号区间筛选所有变更条目
2. 过滤掉非 user-visible 的变更（如纯内部重构、CI 配置、日志格式）
3. 把研发口径的语言改写成用户口径（去掉 CHG-id、文件路径、技术术语）
4. 按「新增 / 优化 / 修复 / 已知问题」分组排版
5. 不能比研发 change-log 多出任何「未提及的功能」

## 推进规则

- 发布计划生成后必须暂停，等待用户确认灰度比例与时间窗口
- 用户确认通过后，按计划推进灰度发布
- 全量发布完成后，进入 19 上线监控与迭代阶段
- 灰度过程中触发回滚条件，视为变更入口，必须先在 `change-plan.md` 中登记，再修代码或回滚
