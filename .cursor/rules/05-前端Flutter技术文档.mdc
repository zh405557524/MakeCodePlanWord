---
description: 前端Flutter技术文档阶段 - 基于技术方案与参考工程生成 Flutter 专项文档
globs:
alwaysApply: false
---

# 前端 Flutter 技术文档规则

## 前置条件

执行本规则前，必须确认：
1. `projects/{currentProject}/02-技术方案/tech-plan.md` 已存在
2. `projects/{currentProject}/01-需求/figma-design-plan.md` 已存在
3. 用户已触发「生成 Flutter 技术文档」或类似指令

## 参考工程

当前 Flutter 文档默认参考工程：

- `G:\code\soul\Solfeggio`

生成文档时优先吸收其真实工程约定：

- `main.dart + Global.init()`
- `GetX`
- `GetStorage + Hive`
- `MaterialApp.router + go_router`
- `flutter_screenutil`
- `CustomScaffold`
- `pages/{feature}/view.dart + controller.dart + index.dart`

## 规则来源（生成前必读）

- `.cursor/rules/flutter/08-项目结构目录.mdc`
- `.cursor/rules/flutter/task-project_init.mdc`
- `.cursor/rules/flutter/task-new_feature.mdc`
- `.cursor/rules/flutter/task-ui_task.mdc`

## 输出路径

- `projects/{currentProject}/03-前端/flutter-tech.md`

## UI 事实来源

生成 Flutter 技术文档时，UI 与交互事实至少按以下顺序综合判断：

1. `projects/{currentProject}/01-需求/requirements.md`
2. `projects/{currentProject}/01-需求/figma-design-plan.md`
3. `projects/{currentProject}/01-需求/references/images/`
4. `projects/{currentProject}/01-需求/references/docs/` 中的颜色与视觉文档

## 文档要求

生成 `flutter-tech.md` 时必须明确：

1. 当前 Flutter 最终技术路线
2. 参考工程对齐点
3. 项目结构
4. 启动与全局初始化
5. 状态管理与数据流
6. 存储设计（GetStorage / Hive）
7. 路由配置
8. UI 基础规范
9. 页面实现摘要
10. 编码任务清单
11. 编码注意事项

其中 UI 基础规范、页面实现摘要、编码任务清单必须明确引用 `figma-design-plan.md` 中的页面、状态、组件和交互要求，而不是只按散图兜底。

## 技术口径

- 默认使用 `GetX`
- 默认使用 `GetStorage + Hive`
- 不再沿用 `Riverpod + Drift` 作为主路线
- 若技术方案与参考工程冲突，优先保持参考工程风格，并在文档中写明
- 若设计计划与单张参考图存在差异，优先以 `figma-design-plan.md` 约束页面与状态要求，并注明图中现状

## 推进规则

- 文档生成完成后，下一步优先进入 Flutter 总体开发计划阶段
