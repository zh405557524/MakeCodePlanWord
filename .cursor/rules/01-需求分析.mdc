---
description: 需求分析阶段 - 引导用户提炼需求，建立项目目录，输出结构化需求清单
globs: 
alwaysApply: false
---

# 需求分析规则

---

## 第零步：入口分流（首轮一站式输入）

用户可能**第一条消息就同时给出**「项目名称 + 图片 + 产品文档」，以减少往返交互。

**处理原则**：
- **已含项目名称** → 直接记为 `currentProject`，**不再询问**项目名称
- **已含图片或产品文档** → 按本步骤处理材料后，**直接输出需求清单**，**禁止**再按 2.1–2.4 逐条追问材料中已有的信息
- **输出节奏**：在同一条或紧随其后的回复中给出「材料摘要（可选）+ 完整结构化需求清单（含来源/推断/待确认）」，把交互留给用户「确认/修改」而非填空式问答

### 材料解读

**若提供图片**（截图、原型图、流程图等）：
- 逐项描述可见信息（页面/模块名称、主流程、关键文案、交互状态）
- 标注不确定处，如「图中未标明权限规则」

**若提供产品文档**（Markdown 正文、Word 粘贴、PDF 要点等）：
- 提取结构化要点（背景、角色、功能列表、业务规则、非功能、术语表）
- 对缺失章节列出**缺口清单**

**若两者都有**：
- 以产品文档为主轴，图片用于校验 UI/流程与文档是否一致
- 冲突处单列「待澄清」，不武断选一方

### 原始材料落盘（必选）

确认 `currentProject` 后，立即**创建目录并写入文件**：

```
projects/{currentProject}/01-需求/references/
├── README.md              # 列出本目录所有文件、对应用途、与需求模块的关联
├── images/                # 截图、原型、流程图
│   └── 01-页面名称.png    # 文件名建议：序号-描述.扩展名
└── docs/                  # 产品文档
    └── product-doc.md     # 粘贴的正文存为 Markdown；按模块可拆多文件
```

规则要点：
- **图片**：写入 `references/images/`，文件名可读有序；同名冲突加序号后缀避免覆盖
- **文档**：用户粘贴的正文 → 写入 `references/docs/product-doc.md`；若用户已在仓库中给出路径，复制并在 README 注明来源
- **后续引用**：后续 PRD、技术方案、前后端文档在引用原型/规则时，优先读取 `references/` 下文件

---

## 第一步：确认项目名称

若消息中**未含**项目名称，询问：

> 请告诉我项目名称（例如：电商App、社交平台、企业管理系统）？
> 若未指定，将使用今天日期命名（如：20260331-未命名项目）

记录为 `currentProject`，后续所有文档路径均使用：
```
projects/{currentProject}/
```

---

## 第二步：自检补全（Agent 内部执行）

对照以下维度自检材料，材料已覆盖的**内部填满**，**不向用户重复提问**；仅记录缺口留入「待确认」节。

仅当**未提供**任何文档/图片时，才可适度引导用户逐维度描述：

#### 2.1 项目背景
- 这个项目要解决什么问题？
- 目标用户是谁？
- 与同类产品相比有什么不同？

#### 2.2 核心功能
- 列举最重要的 3-5 个核心功能
- 每个功能的具体表现是什么？
- 用户如何使用这些功能？

#### 2.3 技术约束
- 前端是否使用 Flutter？目标平台（iOS/Android/Web）？
- 后端是否使用 Kotlin？框架偏好（Ktor/Spring Boot）？
- 数据存储方案？（本地/云端/两者都有）
- 是否有第三方服务集成需求？

#### 2.4 非功能性需求
- 性能要求（响应时间、并发量）？
- 安全要求（用户鉴权方式）？
- 是否需要离线支持？

### 完善策略（材料不完整时）

- 对缺口用**占位 + 假设**写法（如：「假设：单租户；待确认」），禁止无标记的武断结论
- 将缺口收敛为清单中的「待用户确认」节，**不展开多轮追问**
- 仅在**缺少项目名称**或**存在必须二选一的矛盾**时，才发澄清问题（不超过 3 条）

---

## 第三步：输出结构化需求清单

将收集/解读的信息整理为以下格式，先输出清单供用户确认（**不直接生成 PRD**）：

```markdown
## 需求清单 - {currentProject}

### 原始材料路径
- 图片：`projects/{currentProject}/01-需求/references/images/`
- 产品文档：`projects/{currentProject}/01-需求/references/docs/product-doc.md`
（若本次未提供图片或文档，此节可省略）

### 项目概述
- 项目名称：
- 解决问题：（来源：文档/图片/推断）
- 目标用户：（来源：文档/推断）

### 核心功能列表
1. 功能名称 | 描述 | 优先级（高/中/低）| 来源（文档/图片/推断）
2. ...

### 技术约束
- 前端：Flutter（目标平台：）
- 后端：Kotlin（框架：）
- 数据库：
- 第三方集成：

### 非功能性需求
- 性能：
- 安全：
- 离线支持：是/否

### 推断与假设
- （无文档/图片依据的内容，如：「假设：{某业务前提}；待确认」）

### 待用户确认
- [ ] 问题1（P0）
- [ ] 问题2
```

---

## 第四步：确认并推进

输出需求清单后，**等待用户确认**：
- 用户说"确认"、"没问题"、"继续" → 提示用户进入下一阶段（需求文档生成）
- 用户提出修改 → 按要求更新需求清单，重新输出

**推进提示语**（用户确认后输出）：
> 需求清单已确认。下一步：输入"生成需求文档"将需求清单转为完整的 PRD 文档。
