---
description: 需求分析阶段。整理输入材料，输出结构化需求清单。
globs:
alwaysApply: false
---

# 需求分析规则

## 通用执行契约

执行本规则前，必须先完成核心规则中的工作空间定位：

- 读取 `projects/{currentProject}/workflow.md`；若不存在，先按 `.cursor/rules/templates/workflow.md` 创建。
- 读取本规则，以及 `workflow.md` 中“当前步骤依赖文件”列出的材料。
- 执行前先形成本轮计划，再整理或写入需求分析产物。
- 执行后更新 `workflow.md` 的当前阶段、状态、恢复点、依赖文件、生成文件、注意事项和执行记录。
- 若本轮是二次调整，先读取或创建 `projects/{currentProject}/change-plan.md`，默认按 `small_change` 限定影响范围。

## 目标

把用户输入、文档、图片或现有材料整理成一份可确认的“需求清单”，不要直接生成 PRD。

## 输入

- 用户描述
- 原始文档
- 图片/原型
- 已有参考材料

## 基本原则

- 先读材料，再提炼结论。
- 材料里已有的信息不要重复追问。
- 缺失信息用“假设/待确认”标记，不要硬编。
- 只在缺少项目名或存在关键冲突时提问。
- 输出简洁，避免业务绑定和长篇说明。

## 材料处理

- 如果有项目名，记录为 `currentProject`。
- 如果没有项目名，再询问一次。
- 如果有图片或文档，先落盘到：
  - `projects/{currentProject}/01-需求/references/images/`
  - `projects/{currentProject}/01-需求/references/docs/`
- 维护：
  - `projects/{currentProject}/01-需求/references/README.md`

## 输出要求

输出一份“需求清单”，至少包含：

- 项目概述
- 目标用户
- 核心功能
- 关键流程
- 数据与同步要求
- 非功能要求
- 假设与待确认项

建议输出到：

- `projects/{currentProject}/01-需求/requirements-list.md`

## 推进规则

- 输出需求清单后，等待用户确认。
- 用户确认后，再进入“需求文档生成”阶段。
