---
description: Java 编码工作流程规则，仅用于进入 Java 代码阶段后的执行、验证与回写，不负责整体项目流程调度
globs: "**/src/main/java/**/*.java,**/pom.xml,**/src/test/java/**/*.java"
alwaysApply: false
---

# 编码工作流程

> 本规则只负责 **Java 编码阶段内部** 的执行流程。
> 它不是整体项目流程，不负责「需求 → 文档 → 技术方案 → 开发计划」的总调度。
> 整体项目阶段推进仍然由顶层 `00-核心工作流程.mdc` 负责。

---

## 一、适用范围

本规则在以下场景生效：

- 用户要求开始 Java 编码
- 用户要求继续修改 Java 代码
- 用户要求调试、修复、联调 Java 工程
- 当前 `workflow.md` 的当前阶段已经进入 `Java 编码`

不适用场景：

- 需求分析
- PRD 生成或修订
- 技术方案生成
- Java 技术文档生成
- Java 总体开发计划或模块开发文档生成

---

## 二、与整体项目流程的边界

### 2.1 整体项目流程负责什么

整体项目流程由顶层规则负责：

- 当前在哪个阶段
- 是否允许进入下一阶段
- 是否需要暂停等待用户确认
- 是否需要回到文档阶段

### 2.2 Java 编码流程负责什么

本规则只负责：

- 进入 Java 代码阶段后，该先读哪些文档
- 当前编码步骤应该怎么拆解
- 代码实现顺序
- 校验顺序
- 状态回写顺序

---

## 三、编码前必须读取的内容

进入 Java 编码前，至少读取以下内容：

1. `projects/{currentProject}/workflow.md`
2. `projects/{currentProject}/change-plan.md`（顶部「进行中变更」区块）
3. `projects/{currentProject}/04-后端/java-tech.md`
4. `projects/{currentProject}/04-后端/java-dev-plan.md`
5. 当前恢复点对应的 `projects/{currentProject}/04-后端/开发计划/{module}.md`
6. 若涉及前端联动：`projects/{currentProject}/03-前端/flutter-tech.md` 与对应模块文档
7. 若技术文档指定了参考工程，还需读取参考工程的真实结构

---

## 四、编码阶段的执行顺序

### 4.1 先确认当前恢复点

从 `workflow.md` 与 `java-dev-plan.md` 确认：

- 当前步骤编号
- 当前步骤状态
- 当前恢复点文件
- 是否已有 `in_progress` 的编码任务
- `change-plan.md` 是否有 `in_progress` 的变更条目

规则：

- 优先继续 `change-plan.md` 中 `in_progress` 的变更条目
- 其次继续当前 `in_progress` 的开发任务
- 没有 `in_progress` 时，再从第一个依赖满足的 `pending` 步骤继续
- 不得跳过前置依赖直接写后续接口

### 4.2 再做当前步骤的最小实现计划

开始写代码前，先明确本轮只做什么：

- 要改哪些文件
- 要补哪层内容（controller / service / mapper / domain / migration）
- 哪些事情本轮不做

规则：

- 编码阶段仍然按「当前步骤」推进
- 不因为顺手就跨多个步骤大面积扩写

### 4.3 按层次实现

默认实现顺序（自下而上）：

1. 迁移脚本 / Entity 表定义
2. Mapper 方法 + 单元测试
3. Service 方法 + 单元测试
4. Request / Response 模型
5. Controller 注册
6. 集成测试

或新接口场景（自上而下）：

1. Request / Response 模型
2. Controller 占位
3. Service 方法
4. Mapper 方法
5. 迁移脚本（如需要）
6. 测试 + 联调

两种顺序都可以，关键是保持一致，不要在同一个任务中反复跳。

### 4.4 验证

至少做：

- `./mvnw verify`
- `./mvnw test`（必要时）
- 若是接口或链路改动，做最小链路验证（curl / Postman / 集成测试）

### 4.5 回写

编码完成后至少回写：

- `workflow.md`（恢复点、状态、执行记录）
- `java-dev-plan.md`（如任务状态已变化）
- 对应模块开发文档（如模块状态或边界已变化）
- `change-plan.md`（变更条目状态从 `in_progress` 改成 `done`，归档到 `change-log.md`）

---

## 五、不同类型编码任务的默认处理

### 5.1 工程初始化类任务

如当前步骤是初始化或骨架搭建：

- 优先完成目录骨架
- 再完成 `Application.java + config/`
- 再完成 `controller/service/mapper/domain/common` 入口文件
- 最后给出 `/health` 健康检查接口

### 5.2 新接口类任务

如当前步骤是新增业务接口：

- 先确认接口契约（路径、入参、出参、错误码）
- 再补 Request / Response 模型
- 再补 Mapper / Entity（如需要新增表）
- 再补 Service
- 最后补 Controller 与测试

### 5.3 数据迁移类任务

如当前步骤是表结构变更：

- 先写迁移脚本（`V{N}__{描述}.sql`）
- 本地启动验证迁移
- 再改 Entity / Mapper
- 最后改 Service / Controller
- 测试覆盖新旧字段的兼容情况

### 5.4 联调类任务

如当前步骤是联调或 QA：

- 优先保证主链路可点通
- 优先修高频接口与高风险错误码
- 不在 QA 阶段大改基础架构

---

## 六、阻塞式编码阶段规则

Java 编码属于阻塞式代码开发阶段。

含义：

- 只要用户没有明确说「进入下一步」
- 就默认还停留在当前 Java 编码步骤
- 默认继续做当前步骤内的修改、调试、验证和回写

规则：

- 不自动把当前编码步骤标成完成并切下一步
- 不自动从 Java 编码跳到 Flutter 编码或联调 QA
- 只有用户明确推进，或当前步骤已经闭合并需要用户确认时，才允许切换

---

## 七、文档与代码冲突时的优先级

当发现冲突时，处理顺序如下：

1. 当前版本的 `java-tech.md`（接口契约真源）
2. 当前版本的 `tech-plan.md`
3. 当前版本的 `java-dev-plan.md`
4. 当前恢复点模块文档
5. 现有实现代码

规则：

- 若冲突较大，先修文档再修代码
- 不要直接在代码里私自发明新口径
- 接口契约变更必须同步通知前端（更新对应 Flutter 模块文档），并先在 `change-plan.md` 登记

---

## 八、完成判定

当前 Java 编码步骤要满足以下条件，才可认为本轮完成：

- 本轮目标代码已落地
- `./mvnw verify` / `./mvnw test` 已通过或已明确说明无法执行原因
- 数据迁移脚本（如有）已通过本地验证
- 当前步骤相关文档已回写
- 当前步骤的阻塞与风险已记录
- `change-plan.md` 中本轮变更条目（如有）已归档到 `change-log.md`

若以上任一缺失，则本轮仍视为「未完全闭合」。

---

## 九、禁止事项

- 不读取恢复点文档就直接改代码
- 不按当前步骤推进，随意跨步实现
- 不验证就宣称完成
- 不回写 `workflow.md`
- 把 Java 编码流程误当成整体项目流程
- 私自发明前端未对齐的字段或错误码
- 把多条不相关变更塞进同一个 CHG-id
