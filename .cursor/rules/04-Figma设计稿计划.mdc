---
description: Figma 设计稿计划阶段。基于已通过检测的 PRD、参考文档与截图，整理后续补齐设计稿所需的页面、状态、组件与交互计划。
globs:
alwaysApply: false
---

# Figma 设计稿计划规则

## 通用执行契约

执行本规则前，必须先完成核心规则中的工作空间定位：

- 读取 `projects/{currentProject}/workflow.md`；若不存在，先按 `.cursor/rules/templates/workflow.md` 创建。
- 读取本规则，以及 `workflow.md` 中“当前步骤依赖文件”列出的材料。
- 执行前先形成本轮设计计划生成思路，再生成或更新 `figma-design-plan.md`。
- 执行后更新 `workflow.md` 的当前阶段、状态、恢复点、依赖文件、生成文件、注意事项和执行记录。
- 若本轮是二次调整或命中变更管控关键词，先读取或创建 `projects/{currentProject}/change-plan.md`，默认按 `small_change` 限定影响范围。

## 前置条件

执行本规则前，必须确认：

1. `projects/{currentProject}/01-需求/requirements.md` 已存在且已通过检测
2. 用户已明确要求进入“Figma 设计稿计划”或在 PRD 检测通过后说“继续下一步”

如果前置条件不满足，先回到 PRD 生成 / 检测阶段。

## 目标

输出一份可执行的 `figma-design-plan.md`，把 PRD、产品文档、参考图统一整理成后续补齐设计稿的工作清单，避免代码生成只凭零散截图猜界面。

## 输入材料

生成设计计划时至少结合以下材料：

- `projects/{currentProject}/01-需求/requirements.md`
- `projects/{currentProject}/01-需求/references/docs/`
- `projects/{currentProject}/01-需求/references/images/`
- 本轮用户补充的视觉要求、品牌方向或风格限制

## 基本原则

- 设计计划服务于后续 Figma 出稿与代码生成，不写空泛设计口号。
- 没有现成 Figma 链接时，也必须把页面、状态、组件、交互稿要求列完整。
- 页面级设计要求必须能回溯到 PRD 章节或参考图，不凭空新增业务页面。
- 如截图表现与 PRD 目标版本不一致，要同时写明“当前参考表现”与“目标出稿要求”。
- 设计计划生成后必须暂停，等待用户补齐设计稿或明确确认可继续。

## 输出路径

输出到：

- `projects/{currentProject}/01-需求/figma-design-plan.md`

## 固定结构

`figma-design-plan.md` 至少包含以下内容：

1. 文档目的与适用范围
2. 设计目标与视觉方向
3. 页面清单与优先级
4. 页面到 PRD 章节 / 参考图映射
5. 每页必出稿清单（首屏、关键状态、弹层、空态、错误态）
6. 组件与设计 Token 清单
7. 交互 / 原型清单
8. 设计验收标准
9. 待确认项

## 页面清单要求

- 页面清单按 MVP 优先级排序。
- 每个页面至少写清：页面名称、对应 PRD 章节、参考图片、是否需要完整高保真、是否需要交互稿。
- 页面拆分以实际编码页面和关键弹层为单位，不要只写模块名。

## 出稿清单要求

每个页面至少明确以下是否必须出稿：

- 首屏默认态
- 加载态 / 空态 / 错误态
- 关键操作后的反馈态
- 弹窗、抽屉、底部弹层等覆盖层
- 列表项 / 卡片 / 表单等复用结构

## 组件与 Token 要求

- 组件清单至少覆盖导航、按钮、输入、卡片、弹层、状态提示等复用部件。
- Token 清单至少覆盖颜色、字号、间距、圆角、阴影、渐变、图标风格。
- 若已有颜色文档，写明“沿用现有文档”与需要补齐的缺口。

## 交互要求

- 交互 / 原型清单要优先覆盖主路径、跨页跳转和关键状态切换。
- 不要求在本阶段必须提供 Figma 链接，但要写清后续设计稿需补齐哪些交互演示。

## 设计稿产物落点

设计稿在补齐过程中，产物必须按以下落点维护，避免散落到对话或本地缓存：

| 产物类型 | 路径 | 备注 |
|----------|------|------|
| Figma 文件链接 | `figma-design-plan.md` 顶部「Figma 链接表」一节 | 至少含 项目主文件 / 各模块子文件 / 原型链接 |
| 页面静态截图 / 导出图 | `projects/{currentProject}/01-需求/references/images/` | 文件名建议 `{页面英文名}-{状态}.png` |
| 设计稿配套文档 | `projects/{currentProject}/01-需求/references/docs/` | 风格指南、色彩规范、字体规范等 |
| 参考资料索引 | `projects/{currentProject}/01-需求/references/README.md` | 每加一份图片/文档必须在此登记一行 |

设计稿如发生改动，必须按第五步「变更管控」走 `change-plan.md`，并把改动落点同步到 `figma-design-plan.md` 的「设计验收标准」段。

## 推进规则

- 设计计划生成完成后，不要直接进入技术方案阶段。
- 提示用户先根据 `figma-design-plan.md` 补齐设计稿，或明确确认“可按该计划继续”后，再进入技术方案阶段。
