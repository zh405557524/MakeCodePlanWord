---
description: 需求文档生成阶段 - 将需求清单转换为标准 PRD 格式文档，写入项目目录
globs: 
alwaysApply: false
---

# 需求文档生成规则

## 前置条件

执行本规则前，必须确认：
1. 已有 `currentProject`（项目名称）
2. 已有经用户确认的需求清单（来自 01-需求分析.mdc）

若以上信息缺失，提示用户先完成需求分析阶段。

---

## 执行步骤

### 第一步：读取需求清单

读取已确认的需求清单内容，作为 PRD 生成的输入依据。

若需求清单中包含「原始材料路径」，读取 `projects/{currentProject}/01-需求/references/` 下的图片和文档，优先以其为依据生成第 3 章页面结构和第 4 章交互说明，保持与原型/文档一致。

---

### 第二步：生成标准 PRD 文档

按以下模板生成完整的需求文档，输出到：
`projects/{currentProject}/01-需求/requirements.md`

**PRD 文档模板**：

```markdown
# {项目名称} 产品需求文档（PRD）

> 版本：v1.0  
> 创建日期：{今天日期}  
> 状态：草稿

---

## 1. 项目概述

### 1.1 背景与目标
（描述项目背景、要解决的核心问题）

### 1.2 目标用户
（描述主要用户群体及其特征）

### 1.3 产品定位
（与同类产品的差异化优势）

---

## 2. 功能需求

### 2.1 功能模块列表

| 模块 | 功能描述 | 优先级 | 备注 |
|------|---------|--------|------|
| 模块A | ... | 高 | |
| 模块B | ... | 中 | |

### 2.2 核心功能详述

#### 功能1：{功能名称}
- **用户故事**：作为 {用户类型}，我希望 {完成某件事}，以便 {达到某个目标}
- **功能描述**：（详细描述）
- **验收标准**：
  - [ ] 条件1
  - [ ] 条件2
- **边界情况**：（异常处理、限制说明）

（每个核心功能重复以上格式）

---

## 3. UI 页面流与交互结构

> 本章描述整体页面层级、导航结构和各页面区块组成，以原型图/设计稿（references/images/）为准，若无原型则根据功能需求推导。

### 3.1 页面流转

用层级缩进描述页面跳转关系，**示例仅展示结构形态，须按实际产品替换占位符**：

```
开屏页 → {可选：引导/准入等过渡} → {主框架页}
                            ↓
                         {主框架，如：底部 Tab / 侧边栏 / 无 Tab 单栈}
                         ├── {一级分区或 Tab1}
                         │   ├── {子区块1}
                         │   └── {子区块2}
                         ├── {Tab2 或并列入口}
                         └── {…}
```

### 3.2 全局导航结构

| 导航类型 | 描述 | 包含页面 |
|---------|------|---------|
| 底部 Tab 栏 | 全局常驻，共 N 个 Tab | Tab1、Tab2、... |
| 顶部返回栏 | 二级及以上页面显示 | 所有子页面 |
| （其他导航） | | |

### 3.3 页面详细结构

（每个主要页面单独描述）

#### 页面：{页面名称}

- **入口**：（从哪里跳转进来，如：底部 Tab / 点击列表项）
- **布局区块**：
  - 顶部区域：（AppBar / 搜索框 / 轮播图等）
  - 中间内容区：
    - 区块1：{名称} — {功能描述}
    - 区块2：{名称} — {功能描述}
  - 底部区域：（底部导航 / 固定操作按钮 / 无）
- **可交互元素**：（控件/按钮 → 触发行为，用产品实际文案与跳转目标填写）
- **出口**：（点击后可跳转到的页面列表）

（每个主要页面重复以上格式）

---

## 4. 前后端交互说明

> 描述关键操作的完整交互链路。格式：用户操作 → 前端行为 → API 请求 → 后端响应 → 前端处理。
> 细化程度：聚焦用户感知的操作步骤与数据流，覆盖正常流程和主要异常流程，无需描述 UI 样式细节。
> **禁止在规则模板中预填具体业务**（如某固定接口路径、某固定流程名）；一律按 PRD 中的真实功能与接口设计填写。

### 4.1 单条交互的书写结构（模板）

对每一个「需要请求后端」的用户操作，按下列序号展开（条数可按实际增减）：

1. （用户触发的具体操作）
2. （前端即时反馈：校验、弹层、禁用输入、loading 等——按实际写）
3. （若调用接口）→ `{Method} {Path}` — 入参：`{ … }`
4. （后端处理要点与返回数据结构摘要）
5. （前端对成功 / 业务失败 / 网络或系统异常的分支处理）

### 4.2 按功能模块组织

#### 模块：{模块名称}

##### 交互：{该模块下某一具体操作名称}
（将 4.1 的 1～5 步填完整）

##### 交互：{…}
（同上）

（按产品实际模块与操作逐一补充；凡涉及网络请求的，均应有对应一条交互说明）

---

## 5. 非功能性需求

### 5.1 性能要求
- 页面加载时间：
- 接口响应时间：
- 并发支持：

### 5.2 安全要求
- 用户鉴权方式：
- 数据加密要求：
- 权限控制：

### 5.3 兼容性要求
- 前端支持平台：（iOS / Android / Web）
- 最低系统版本：
- 离线支持：是/否

---

## 6. 技术约束

- **前端技术栈**：Flutter {版本}，目标平台：{平台列表}
- **后端技术栈**：Kotlin，框架：{框架名称}
- **数据存储**：{数据库方案}
- **第三方集成**：{集成服务列表}

---

## 7. 里程碑规划

| 阶段 | 内容 | 预计完成 |
|------|------|---------|
| 阶段一 | MVP 核心功能 | |
| 阶段二 | 完整功能 | |
| 阶段三 | 优化上线 | |

---

## 8. 待确认事项

> 以下内容需要进一步确认：
- [ ] 待确认项1
- [ ] 待确认项2
```

---

### 第三步：写入文件

将生成的 PRD 内容写入：
```
projects/{currentProject}/01-需求/requirements.md
```

若目录不存在，先创建目录结构。

---

### 第四步：推进到检测阶段

文件写入完成后输出：

> PRD 文档已生成：`projects/{currentProject}/01-需求/requirements.md`
> 
> 下一步：输入"检测需求文档"进行文档质量检测。
