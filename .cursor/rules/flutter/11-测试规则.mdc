---
description: Flutter 测试规则，面向测试人员的测试用例编写、执行记录与 Python 自动化辅助执行规范
globs: "**/qa/**/*.md,**/05-联调与测试/**/*.md,**/scripts/test/**/*.py,**/tools/test/**/*.py"
alwaysApply: false
---

# 测试规则

> 本规则面向 **测试人员的测试用例设计与执行**。  
> 它不是 Dart 单元测试、Widget 测试或集成测试规则。  
> Python 在这里的作用是辅助执行测试步骤、复现操作、生成截图和做冒烟回归，不是替代测试用例本身。

---

## 一、适用范围

本规则在以下场景生效：

- 新功能上线前，需要编写测试人员执行的测试用例
- Bug 修复后，需要补回归用例
- 需要整理提测范围、主链路、异常链路和验收口径
- 需要用 Python 模拟点击、输入、切换、截图来辅助执行测试

不适用场景：

- 纯代码层测试规范
- Flutter 单元测试或 Widget 测试模板
- 项目级排期、提测流程和缺陷优先级制度

---

## 二、测试用例的目标

测试用例的目标不是“证明代码写了”，而是：

1. 让测试人员知道要测什么
2. 让不同测试人员执行时结果一致
3. 让开发、产品、测试对通过标准一致
4. 让回归测试可以重复执行

规则：

- 用例必须可执行
- 用例必须可复现
- 用例必须有明确预期结果
- 用例必须能区分主流程、异常流程和边界流程

---

## 三、测试用例分类

Flutter 页面与功能默认按以下分类组织测试用例：

1. **主流程用例**
   - 验证核心功能能否按预期跑通
   - 例如：创建文件夹、创建笔记、进入详情、保存内容

2. **异常用例**
   - 验证错误输入、失败状态、无权限、网络异常等情况
   - 例如：标题为空时保存失败、接口超时时提示重试

3. **边界用例**
   - 验证极限长度、空列表、重复数据、深层嵌套等边界
   - 例如：超长标题、空文件夹、10 级子文件夹

4. **状态用例**
   - 验证 `loading / empty / error / success / disabled / not_found`
   - 例如：首次加载中、空数据页、加载失败页

5. **兼容与体验用例**
   - 验证屏幕尺寸、深浅主题、滚动、键盘遮挡、返回行为
   - 例如：小屏设备输入框被键盘遮挡、长列表滚动卡顿

6. **回归冒烟用例**
   - 验证最核心链路没有被新改动破坏
   - 例如：首页进入 Notes，新建笔记，保存成功，返回列表可见

---

## 四、测试用例编写模板

每条测试用例至少包含以下字段：

| 字段 | 说明 |
|------|------|
| 用例 ID | 唯一编号，如 `NOTES-001` |
| 模块 | 所属模块或页面 |
| 用例标题 | 用一句话说明测试目标 |
| 优先级 | `P0 / P1 / P2 / P3` |
| 前置条件 | 执行前必须满足的环境与数据 |
| 测试步骤 | 测试人员实际执行的操作 |
| 预期结果 | 每步或最终应该看到的结果 |
| 测试数据 | 账号、文案、输入值、样例数据 |
| 备注 | 截图点、风险点、自动化说明 |

推荐模板：

```md
### NOTES-001 新建笔记成功

- 模块：Notes / NoteEditor
- 优先级：P0
- 前置条件：
  - 用户已登录
  - 当前已进入 Notes 首页
- 测试数据：
  - 标题：测试笔记001
  - 正文：这是一个测试正文
- 测试步骤：
  1. 点击“新建笔记”
  2. 输入标题“测试笔记001”
  3. 输入正文“这是一个测试正文”
  4. 点击“保存”
- 预期结果：
  1. 成功进入编辑页
  2. 标题输入框显示输入内容
  3. 正文输入区域显示输入内容
  4. 保存成功并返回列表，列表中可看到“测试笔记001”
- 备注：
  - 保存前后各截一张图
  - 可用 Python 冒烟脚本辅助执行
```

---

## 五、测试用例编写规则

### 5.1 一个功能至少要有什么

每个功能默认至少包含：

1. 一条主流程成功用例
2. 一条异常用例
3. 一条边界或状态用例

### 5.2 用例标题怎么写

推荐格式：

- `创建文件夹成功`
- `文件夹名称为空时创建失败`
- `Markdown 预览模式切换成功`
- `空文件夹展示空状态`

规则：

- 标题描述“行为 + 结果”
- 不写模糊标题，如 `测试一下 Notes`
- 一条用例只验证一个核心目标

### 5.3 步骤怎么写

规则：

- 步骤必须是测试人员可直接照着操作的动作
- 每一步只写一个动作
- 不要把多个动作合并成一句
- 能点哪里、输什么、切到哪一页，要写清楚

错误示例：

`进入页面后随便操作一下并查看结果`

正确示例：

1. 点击底部导航“Notes”
2. 点击“新建笔记”
3. 在标题输入框输入“测试笔记001”
4. 点击“保存”

### 5.4 预期结果怎么写

规则：

- 预期结果必须可观察
- 尽量写成页面可见结果、提示文案、按钮状态、跳转结果
- 不写“接口应正确返回”这种测试人员不可直接观察的结果，除非配合抓包或日志要求

错误示例：

`系统处理正常`

正确示例：

`页面提示“保存成功”，并返回笔记列表；列表首项显示“测试笔记001”`

---

## 六、测试覆盖维度

写 Flutter 页面测试用例时，默认至少从这些维度检查：

1. 页面入口是否可达
2. 页面标题、按钮、输入框、列表是否正确显示
3. 主功能是否成功执行
4. 空数据时是否有空状态
5. 失败时是否有错误提示或重试入口
6. 返回、关闭、取消是否符合预期
7. 键盘弹起后输入区域是否可见
8. 滚动、长文本、超长列表是否正常
9. 多层页面跳转后状态是否保持正确
10. 重新进入页面时数据是否正确恢复

如果是表单或编辑器，还要额外覆盖：

1. 必填校验
2. 输入长度限制
3. 特殊字符
4. 草稿/未保存离开
5. 编辑与预览切换

---

## 七、测试用例优先级

- `P0`：核心链路，失败会导致功能不可用
- `P1`：主要功能，失败会明显影响用户使用
- `P2`：边界、异常、兼容性
- `P3`：体验优化、低频场景

规则：

- 每个页面至少要有 `P0` 和 `P1` 用例
- 新功能提测时，先保证 `P0/P1` 覆盖完整
- 回归测试优先执行 `P0/P1`

---

## 八、测试用例组织方式

推荐目录：

```text
05-联调与测试/
├── qa-plan.md
├── cases/
│   ├── notes.md
│   ├── plan.md
│   └── profile.md
└── defects/
```

推荐组织方式：

- 一个模块一个 `cases/{module}.md`
- 每个页面按“主流程 / 异常 / 边界 / 回归”分组
- 冒烟用例可以单独维护成一节

---

## 九、Python 辅助测试规则

### 9.1 Python 的定位

Python 在这里用于：

- 模拟点击、输入、切换、滚动
- 自动执行固定冒烟步骤
- 自动截图
- 帮测试人员快速复现标准用例

不用于：

- 代替测试用例文档
- 代替完整人工验收
- 把所有复杂断言都硬编码成脚本

### 9.2 工具优先级

优先顺序：

1. `playwright`
   - 用于 Web 页面、H5、Flutter Web
   - 有稳定选择器时优先使用
2. `pyautogui`
   - 用于桌面窗口、模拟器、没有可用选择器的界面

规则：

- 能用选择器就不要用坐标
- 能显式等待就不要大量写 `sleep`
- 自动化脚本必须和测试用例 ID 对应

### 9.3 Playwright 示例

```python
from pathlib import Path

from playwright.sync_api import sync_playwright


def run_notes_001():
    artifact_dir = Path("artifacts/tests")
    artifact_dir.mkdir(parents=True, exist_ok=True)

    with sync_playwright() as p:
        browser = p.chromium.launch(headless=False)
        page = browser.new_page(viewport={"width": 390, "height": 844})

        page.goto("http://127.0.0.1:3000", wait_until="networkidle")
        page.get_by_role("button", name="新建笔记").click()
        page.get_by_placeholder("请输入标题").fill("测试笔记001")
        page.get_by_role("button", name="保存").click()
        page.screenshot(path=str(artifact_dir / "NOTES-001.png"), full_page=True)

        browser.close()
```

### 9.4 PyAutoGUI 示例

```python
from pathlib import Path
import time

import pyautogui


def run_notes_001():
    artifact_dir = Path("artifacts/tests")
    artifact_dir.mkdir(parents=True, exist_ok=True)

    time.sleep(2)  # 给测试人员时间切到目标窗口

    pyautogui.click(220, 110)
    pyautogui.write("测试笔记001", interval=0.05)
    pyautogui.press("tab")
    pyautogui.write("这是一个测试正文", interval=0.03)
    pyautogui.screenshot(str(artifact_dir / "NOTES-001.png"))
```

### 9.5 Python 脚本约束

- 一个脚本最好对应一条或一组强相关用例
- 脚本命名建议带用例 ID
- 脚本执行前要写清楚前置条件
- 脚本执行后要输出截图或日志
- 脚本失败时要能让测试人员判断卡在哪一步

推荐命名：

- `notes_001_create_note.py`
- `plan_003_empty_state.py`
- `profile_smoke.py`

---

## 十、执行记录规则

测试人员执行用例后，至少记录：

| 字段 | 说明 |
|------|------|
| 执行人 | 谁执行的 |
| 执行时间 | 什么时间执行 |
| 执行版本 | 测试的版本号或构建号 |
| 执行结果 | `passed / failed / blocked` |
| 缺陷单 | 如失败，对应缺陷 ID |
| 备注 | 截图、录屏、环境说明 |

规则：

- 用例执行失败必须能追到缺陷记录
- 用例被阻塞要写清楚阻塞原因
- 冒烟用例建议每轮发版前至少执行一遍

---

## 十一、完成判定

当前测试规则落地完成，至少意味着：

- 已经有可执行的测试用例模板
- 用例能覆盖主流程、异常、边界和状态
- Python 自动化脚本只作为辅助，不替代用例文档
- 测试人员可以根据文档直接执行，不需要靠口头补充

---

## 十二、禁止事项

- 把测试用例写成开发任务清单
- 只写标题，不写步骤和预期结果
- 只写 happy path，不写异常和边界
- 用“系统正常”“结果正确”这种不可观察描述代替预期结果
- 用 Python 脚本代替测试用例文档
- 大量依赖固定坐标和固定 `sleep` 却不写前置条件
