---
description: 前端Flutter技术文档阶段 - 基于技术方案生成Flutter专项详细技术文档，供Cursor直接执行编码
globs: 
alwaysApply: false
---

# 前端 Flutter 技术文档规则

## 前置条件

执行本规则前，必须确认：
1. `projects/{currentProject}/02-技术方案/tech-plan.md` 已存在
2. 用户已触发"生成Flutter技术文档"或类似指令

---

## 执行步骤

### 第一步：读取技术方案

读取 `projects/{currentProject}/02-技术方案/tech-plan.md`，提取：
- 前端页面路由规划
- 前端模块列表和优先级
- 状态管理方案选型
- API 接口列表（前端需对接的部分）
- Flutter 版本和目标平台

---

### 第二步：生成 Flutter 技术文档

输出到：`projects/{currentProject}/03-前端/flutter-tech.md`

**文档模板**：

```markdown
# {项目名称} Flutter 前端技术文档

> 版本：v1.0  
> 创建日期：{今天日期}  
> 关联技术方案：../02-技术方案/tech-plan.md  
> Flutter 版本：{版本}  
> 目标平台：{iOS / Android / Web}

---

## 1. 项目结构

```
lib/
├── main.dart                 # 入口文件
├── app/
│   ├── routes/               # 路由配置
│   └── bindings/             # 依赖注入（GetX）
├── core/
│   ├── network/              # 网络请求封装
│   ├── storage/              # 本地存储封装
│   ├── theme/                # 主题配置
│   └── utils/                # 工具类
├── data/
│   ├── models/               # 数据模型
│   ├── repositories/         # 数据仓库
│   └── providers/            # API 请求
└── pages/
    ├── {模块名}/
    │   ├── index.dart        # 模块入口（统一导出）
    │   ├── view.dart         # UI 视图
    │   ├── controller.dart   # 业务逻辑
    │   └── binding.dart      # 依赖绑定
    └── ...
```

---

## 2. 依赖包清单

```yaml
dependencies:
  flutter:
    sdk: flutter
  # 状态管理
  get: ^4.x.x          # GetX 状态管理 + 路由
  # 网络请求
  dio: ^5.x.x
  # 本地存储
  get_storage: ^2.x.x
  # （根据需求补充）

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.x.x
```

---

## 3. 路由配置

| 路由名 | 路径 | 页面类 | 说明 |
|--------|------|--------|------|
| Routes.login | /login | LoginPage | 登录页 |
| Routes.home | /home | HomePage | 首页 |
（按实际规划填写）

---

## 4. 各模块开发规范

### 4.1 页面模块结构（GetX 模式）

每个页面模块必须包含：
- `view.dart`：只负责 UI，通过 `controller.xxx` 读取数据
- `controller.dart`：所有业务逻辑和状态，继承 `GetxController`
- `binding.dart`：依赖注入，注册 Controller
- `index.dart`：统一 `export` 以上三个文件

### 4.2 网络请求规范

- 统一使用 `Dio` 封装的 `ApiClient`
- 请求/响应拦截器处理 Token 和错误
- Repository 层封装 API 调用，页面只调用 Repository
- 接口返回统一解析为 `Result<T>` 类型

### 4.3 状态管理规范

- 响应式变量使用 `.obs`
- 列表数据使用 `RxList<T>`
- 加载状态统一使用 `isLoading.obs`
- 错误信息统一使用 `errorMsg.obs`

---

## 5. 各页面详细设计

（按技术方案中的页面列表逐一展开）

### 页面：{页面名称}

**路由**：`/{路由路径}`  
**功能描述**：（对应 PRD 中的哪个功能）

**UI 结构**：
```
页面布局描述（Scaffold → AppBar → Body 结构）
```

**Controller 方法清单**：
| 方法名 | 参数 | 返回值 | 说明 |
|--------|------|--------|------|
| onInit() | - | void | 初始化数据加载 |
| fetchData() | - | void | 获取列表数据 |
| onItemTap(id) | String | void | 点击列表项 |

**API 依赖**：
- `GET /api/xxx` — 获取数据

---

## 6. 编码任务清单

供 Cursor 按序执行，每完成一项打勾：

- [ ] T-F001：搭建项目基础结构（路由、网络、存储封装）
- [ ] T-F002：实现登录/注册页面及鉴权逻辑
- [ ] T-F003：实现首页（{描述}）
- [ ] T-F004：实现{模块名}页面
- [ ] T-F005：集成测试和错误处理优化

（按实际任务拆分补充）

---

## 7. 编码注意事项

- **引用规范**：只通过模块 `index.dart` 导入，禁止直接引用内部文件
- **命名规范**：文件名用下划线，类名用大驼峰，变量用小驼峰
- **注释要求**：所有 public 方法必须写 `///` 文档注释（中文）
- **禁止使用** `Get.context` 获取 BuildContext，通过 Widget 传入
- **异步方法**：统一使用 `async/await`，配合 `try/catch` 处理异常
```

---

### 第三步：推进提示

文件写入完成后输出：

> Flutter 技术文档已生成：`projects/{currentProject}/03-前端/flutter-tech.md`
> 
> 下一步可输入：
> - `"生成Kotlin技术文档"` — 生成后端详细技术文档
> - `"开始Flutter编码"` — 按技术文档中的任务清单开始编写 Flutter 代码
