---
description: Kotlin 鉴权与安全规则，覆盖 JWT、密码哈希、SQL 注入防护、CORS、限流与敏感数据处理
globs: "**/src/main/kotlin/**/*.kt"
alwaysApply: false
---

# 鉴权与安全

## 一、鉴权方式

默认 JWT Bearer Token。

### 1.1 Token 设计

- HS256 + 32 字节以上密钥（生产环境密钥必须经环境变量注入）
- payload 至少包含：`userId`、`exp`、`iat`、`iss`、`aud`
- 不在 Token 中放敏感信息（手机号、邮箱、身份证）
- 默认有效期：7 天；可配置；过期前 1 天允许刷新

### 1.2 Refresh Token（可选）

- 长期 Refresh Token 单独签发，存数据库
- 一次性使用：每次刷新 Refresh Token 后失效旧 Token
- 用户登出时撤销 Refresh Token

### 1.3 黑名单 / 注销

- 用户主动注销或修改密码时，必须能立即让旧 Token 失效
- 实现方案：
  - Token 中携带 `version` 字段，用户表存 `tokenVersion`，鉴权时比对
  - 或建立 Redis 黑名单（短 Token + 黑名单时长）

---

## 二、密码处理

- 必须用 BCrypt（或 Argon2、scrypt），禁止 MD5 / SHA1
- BCrypt 推荐 cost = 10 ~ 12
- 密码哈希字段名：`passwordHash`
- 禁止明文存储、禁止把密码写入日志

### 2.1 注册时

```kotlin
val passwordHash = BCrypt.hashpw(req.password, BCrypt.gensalt(12))
```

### 2.2 登录时

```kotlin
if (!BCrypt.checkpw(req.password, user.passwordHash)) {
    throw UnauthorizedException("invalid credentials")
}
```

### 2.3 密码强度

- 最小长度：8（默认）
- 推荐：至少包含字母 + 数字
- 高安全场景：要求字母 + 数字 + 符号 + 大小写
- 在 Request 校验阶段统一处理，不在 Service 重复

---

## 三、SQL 注入防护

- 永远不要字符串拼接 SQL
- 用 Exposed DSL：`Users.select { Users.email eq email }`
- 用原生 SQL 时必须参数化：`exec("SELECT * FROM users WHERE id = ?", listOf(IntColumnType() to id))`
- 复杂动态查询通过 Exposed `andWhere { }` / `orWhere { }` 组合

---

## 四、CORS

仅在确实需要跨域时启用：

```kotlin
install(CORS) {
    anyHost()       // 仅开发环境
    allowHost("app.example.com", schemes = listOf("https"))  // 生产环境
    allowMethod(HttpMethod.Options)
    allowMethod(HttpMethod.Put)
    allowMethod(HttpMethod.Delete)
    allowHeader(HttpHeaders.Authorization)
    allowHeader(HttpHeaders.ContentType)
    allowCredentials = true
    maxAgeInSeconds = 3600
}
```

约定：

- 生产禁止 `anyHost()`
- 仅放行明确域名 + 必要方法 + 必要头部

---

## 五、限流

防刷：

- 登录、注册、发送验证码：每 IP / 每账号每分钟 N 次
- 通用接口：每用户每秒 N 次

实现方案：

- Resilience4j RateLimiter（进程内）
- Redis + Token Bucket（分布式）

超限响应统一 429：

```kotlin
throw BusinessException(code = 4029, message = "Too many requests")
```

---

## 六、敏感数据脱敏

日志中：

- 手机号：`138****1234`
- 邮箱：`a***@example.com`
- 身份证：`110***********1234`
- 银行卡：`6222 **** **** 1234`

实现：在 `utils/Mask.kt` 统一封装，所有日志输出必须先脱敏。

---

## 七、HTTPS 与传输

- 所有线上环境必须 HTTPS
- 后端服务通常运行在 HTTP（由前置代理终结 TLS），但 `Application.kt` 必须信任 `X-Forwarded-Proto`
- WebSocket 必须用 WSS
- 内部服务间通信也应启用 mTLS（高安全场景）

---

## 八、依赖与漏洞扫描

- 集成依赖审计工具（`dependencyCheck` / `snyk`）
- 每月跑一次扫描，发现高危漏洞立即升级
- 升级核心依赖必须走 `change-plan.md`

---

## 九、敏感配置

- 不允许把密钥、Token、密码硬编码在源码
- 不允许 `.env` / `application.conf` 中真实生产配置入仓库
- 所有敏感配置走环境变量或密钥管理（AWS Secrets Manager、Vault、KMS）

---

## 十、审计日志

至少记录以下操作：

- 登录成功 / 失败（含 IP、UA）
- 密码修改、Token 失效
- 权限变更
- 资金 / 订单类核心写操作
- 数据导出

审计日志单独输出到 `logs/audit.log`，保留 ≥ 90 天，独立于普通业务日志。

---

## 十一、越权防护

- 所有「按 ID 查 / 改 / 删」接口必须验证：「当前用户」是否有权操作「该 ID」
- 不允许仅靠路径参数信任用户身份
- Service 层强制校验：`if (user.id != target.ownerId && !user.isAdmin) throw ...`

---

## 十二、敏感操作二次验证

- 修改密码、绑定手机、删除账号等敏感操作必须二次验证（短信码 / 邮箱码 / 二次输入密码）
- 验证码：6 位数字，5 分钟有效，单次使用，绑定操作场景
