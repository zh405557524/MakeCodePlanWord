---
description: Kotlin 数据库与持久化规则，覆盖 Exposed DSL / DAO 选择、迁移脚本、事务边界与索引策略
globs: "**/src/main/kotlin/**/repositories/**/*.kt,**/src/main/kotlin/**/models/entity/**/*.kt,**/src/main/resources/db/migration/**/*.sql"
alwaysApply: false
---

# 数据库与持久化

## 一、ORM 选型

默认使用 Exposed：

- DSL：函数式查询，适合复杂查询
- DAO：实体面向对象，适合简单 CRUD

混用规则：

- 同一表只用一种风格，不允许 DSL 与 DAO 在同一仓库随意混
- 项目级风格在 `kotlin-tech.md` 中明确

---

## 二、实体定义

### 2.1 DSL Table 风格

```kotlin
object Users : Table("users") {
    val id = uuid("id").autoGenerate()
    val username = varchar("username", 50).uniqueIndex()
    val email = varchar("email", 100).uniqueIndex()
    val passwordHash = varchar("password_hash", 255)
    val createdAt = timestamp("created_at").defaultExpression(CurrentTimestamp())
    val updatedAt = timestamp("updated_at").defaultExpression(CurrentTimestamp())

    override val primaryKey = PrimaryKey(id)
}
```

### 2.2 DAO 风格

```kotlin
object Users : UUIDTable("users") {
    val username = varchar("username", 50).uniqueIndex()
    val email = varchar("email", 100).uniqueIndex()
    val passwordHash = varchar("password_hash", 255)
    val createdAt = timestamp("created_at").defaultExpression(CurrentTimestamp())
    val updatedAt = timestamp("updated_at").defaultExpression(CurrentTimestamp())
}

class UserEntity(id: EntityID<UUID>) : UUIDEntity(id) {
    companion object : UUIDEntityClass<UserEntity>(Users)

    var username by Users.username
    var email by Users.email
    var passwordHash by Users.passwordHash
    var createdAt by Users.createdAt
    var updatedAt by Users.updatedAt
}
```

---

## 三、Repository 标准模式

```kotlin
class UserRepository {

    suspend fun findByEmail(email: String): User? = newSuspendedTransaction {
        Users.select { Users.email eq email }
            .map { it.toUser() }
            .singleOrNull()
    }

    suspend fun create(user: User, passwordHash: String): User = newSuspendedTransaction {
        val id = Users.insertAndGetId {
            it[username] = user.username
            it[email] = user.email
            it[Users.passwordHash] = passwordHash
        }
        user.copy(id = id.value)
    }

    private fun ResultRow.toUser(): User = User(
        id = this[Users.id].value,
        username = this[Users.username],
        email = this[Users.email],
    )
}
```

约定：

- IO 操作必须包在 `newSuspendedTransaction { }` 内
- 单条结果用 `singleOrNull`，避免 `.first()` 在空集合上抛
- 实体映射函数（`toUser()`）放在 Repository 内私有扩展

---

## 四、事务边界

| 场景 | 事务位置 |
|------|----------|
| Repository 内单步写 | Repository 内 `transaction { }` |
| Service 跨 Repository 写 | Service 内 `transaction { repoA.x(); repoB.y() }` |
| 多个独立写、无强一致性要求 | 不共享事务，各自独立 |

禁止：

- 在 Routes 层打开事务
- 跨请求复用事务
- 在协程内嵌套打开新事务

---

## 五、迁移脚本

使用 Flyway（默认）或 Liquibase。

### 5.1 命名规则

```
V{版本号}__{描述}.sql
例：
V1__init.sql
V2__add_users_avatar.sql
V3__create_orders_table.sql
```

### 5.2 强约束

- 一旦版本号文件提交并发布到任何环境，**不允许修改**
- 修改字段或表必须新增下一个版本号文件
- 删除字段或表必须在迁移脚本中标注影响范围，并在 `change-log.md` 登记
- 大表迁移必须分批，使用 `ALTER ... ALGORITHM=INPLACE` 等在线 DDL（如数据库支持）

### 5.3 数据迁移

- 业务数据修复脚本与结构迁移脚本分开
- 数据修复脚本用 `R__{描述}.sql`（Flyway repeatable）或单独工具脚本

---

## 六、索引策略

- 主键：UUID（推荐）或自增 ID
- 唯一约束：`uniqueIndex()` 显式声明
- 查询条件字段必须建索引（`index()`）
- 多列联合索引按最常用顺序排列
- 超过 3 列的联合索引必须在 `kotlin-tech.md` 中说明用途

---

## 七、缓存

如引入 Redis 或本地缓存：

- 缓存键命名：`{project}:{domain}:{key}`，如 `app:user:123`
- 缓存层位于 Repository 之上，由 Service 决定何时读 / 写 / 失效
- 严禁在 Routes 直接操作缓存

---

## 八、连接池

- 默认 HikariCP
- 池大小：核心数 × 2 + 1（CPU 密集）或更高（IO 密集）
- 超时配置：connectionTimeout、idleTimeout、maxLifetime 必须显式配置，不使用默认

---

## 九、SQL 注入防护

- 永远不要用字符串拼接构造 SQL
- 所有用户输入必须走 Exposed DSL / 参数化查询
- 必须用原生 SQL 时，使用 `exec` + 参数列表
- 复杂搜索必须在 Repository 内部封装，不向上层暴露原生 SQL

---

## 十、慢查询与监控

- 在 `application.conf` 配置慢查询阈值（如 200ms）
- 慢查询日志单独输出，便于审计
- 监控接口 P95 / P99 响应时长，回流到 19 上线监控阶段
