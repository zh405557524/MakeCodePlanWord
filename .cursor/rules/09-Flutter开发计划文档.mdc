---
description: Flutter 总体开发计划阶段 - 基于 flutter-tech.md 生成可断点恢复的总体开发计划
globs:
alwaysApply: false
---

# Flutter 总体开发计划规则

## 通用执行契约

执行本规则前，必须先完成核心规则中的工作空间定位：

- 读取 `projects/{currentProject}/workflow.md`；若不存在，先按 `.cursor/rules/templates/workflow.md` 创建。
- 读取本规则，以及 `workflow.md` 中“当前步骤依赖文件”列出的材料。
- 执行前先形成本轮总体开发计划，再生成或更新 Flutter 总体开发计划文档。
- 执行后更新 `workflow.md` 的当前阶段、状态、恢复点、依赖文件、生成文件、注意事项和执行记录。
- 若本轮是二次调整或命中变更管控关键词，先读取或创建 `projects/{currentProject}/change-plan.md`，默认按 `small_change` 限定影响范围。

## 前置条件

执行本规则前，必须确认：
1. `projects/{currentProject}/03-前端/flutter-tech.md` 已存在
2. `projects/{currentProject}/02-技术方案/effort-estimate.md` 已存在或用户明确跳过
3. 用户已明确要求生成 Flutter 开发计划或总体开发计划文档

## 参考工程

参考工程不再硬编码到规则中，而是由项目级配置驱动：

- `workflow.md` 中的 `referenceProject` 字段指向真实的参考工程绝对路径（如已设置）。
- 若未设置或目录不存在，按 `.cursor/rules/flutter/` 通用规则库继续，不引用不存在的工程。

开发顺序与工程口径优先继承 `referenceProject` 的真实模式，不再沿用旧的 `Riverpod + Drift` 计划。

## 目标

输出一份直接给 AI 执行 Flutter 编码的总体开发计划文档，满足：

- 先有整体计划，再拆模块计划
- 文档可反复执行
- 文档内明确当前恢复点
- 中断后能从恢复点继续
- 总体计划生成后先等待用户审核

## 输出路径

- `projects/{currentProject}/03-前端/flutter-dev-plan.md`

## 文档要求

总体开发计划至少包含：

1. 文档用途
2. 当前状态与恢复点
3. 状态枚举与更新规则
4. 总体开发策略
5. 总体阶段划分（含工时与里程碑，引用 `effort-estimate.md`）
6. 详细任务清单（任务 ID 与技术方案 `T-F00x` 对齐）
7. 模块开发文档列表
8. 编码约束
9. 执行记录

## 业务逻辑与接口约束

生成 Flutter 总体开发计划时，必须明确前端开发文档对应的业务边界和业务逻辑，不允许只列页面和 UI 任务。

- 若 `projects/{currentProject}/04-后端/kotlin-tech.md` 已存在，前端业务逻辑必须优先参考其中的后端接口、请求体、响应体、错误码和状态流转。
- 总体开发计划中的业务任务必须能追溯到具体后端接口或本地存储逻辑，例如作品创建、文本解析、任务轮询、角色/场景保存、分镜更新、视频生成、分享链接等。
- 对复杂业务链路，必须在“模块开发文档列表”中规划对应业务模块开发文档，例如首页与故事解析、角色与场景、分镜处理、视频预览、联调验收。
- 业务模块开发文档需要明确 Controller、Service、API、Store、本地缓存、异常处理和 Figma 验收之间的关系。
- 如果后端接口已经明确，不得在前端计划中另行发明不一致的接口路径、字段名或状态枚举；如发现冲突，应先修订文档再进入编码。
- 对需要前端本地先行处理的逻辑，例如草稿保存、乐观更新、任务轮询恢复，必须说明它与后端接口的同步关系。

## 技术口径

- 按 `GetX + GetStorage + Hive + Global.init() + go_router + CustomScaffold`
- 恢复点与任务必须贴合该技术路线
- 不写 `Provider / Riverpod / Drift` 风格任务名

## 推进规则

- 总体开发计划生成后必须暂停
- 等用户明确审核通过后，再进入 11 Flutter 模块开发文档阶段
- 后端 Kotlin 总体开发计划（10）可与本规则并行生成
