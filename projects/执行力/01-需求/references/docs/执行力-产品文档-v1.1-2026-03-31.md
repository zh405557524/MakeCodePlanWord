# 执行力 · 产品文档 v1.2
> 更新时间：2026年5月11日

---

## 一、产品概述

| 字段 | 内容 |
|------|------|
| 应用名称 | 执行力 |
| 核心哲学 | 始终显示「现在应该做什么」，而不是「所有可以做什么」 |
| 版本 | v1.2 |
| 定位 | AI 驱动的个人计划执行系统 |
| 端 | 移动优先 Web App (max-width 480px) |

### 技术栈

- **框架**：React 18 + TypeScript
- **样式**：Tailwind CSS v4
- **路由**：React Router (Data Mode)
- **动画**：Motion (motion/react)
- **图表**：Recharts
- **图标**：lucide-react
- **持久化**：localStorage

---

## 二、路由架构

```
/ ─────────────── Now (当前任务)
/chat ─────────── Chat (AI 对话，全屏)
/plan ─────────── Plan (计划管理)
/notes ────────── Notes (笔记首页)
/notes/folder/:id ─ NoteFolder (子文件夹)
/notes/file/:id ─ NoteFile (笔记文件)
/plan/battle ──── BattleMap (年度作战地图)
/plan/battle/:id ─ TrackDetail (主线详情)
/plan/editor ───── PlanEditor (新建计划)
/plan/editor/:id ── PlanEditor (编辑计划)
/profile ────────── Profile (个人中心)
/profile/me ─────── UserProfile (个人信息)
/doc ────────────── ProductDoc (本文档)
```

### 5-Tab 导航

| Tab | 路径 | 图标 | 说明 |
|-----|------|------|------|
| Now | / | Zap | 当前任务 |
| Chat | /chat | MessageCircle | AI 对话 |
| Plan | /plan | Calendar | 计划管理 |
| Notes | /notes | FileText | 笔记与资料沉淀 |
| Me / Profile | /profile | User | 个人中心 |

> 以下页面隐藏底部导航栏：/chat、/plan/battle/*、/plan/editor/*、/profile/me、/notes/file/*

### v2 对话主入口路由草案（2026-06-05）

v2 Figma 设计稿以对话主入口为新默认体验。以下为本轮设计重构口径，不影响 v1 已实现页面的历史追溯。

```text
/ ───────────────── ChatHome (对话主入口，唯一一级页面)
/today/focus ────── TodayFocus (二级全屏专注页)
/plan/create ────── PlanCreate (由对话或工具菜单进入)
/notes ──────────── NotesEntry (由对话或工具菜单进入)
/profile ────────── ProfileSettings (个人资料与偏好)
ChatHome + ──────── ToolMenu (工具菜单 overlay)
ChatHome menu ───── SideMenu (历史 / 计划库 / 笔记 / 设置)
```

v2 导航原则：

- `ChatHome` 保留底部 composer，是打开 App 后的默认页面。
- `TodayFocus` 不显示 composer、底部导航、侧边菜单或任务列表。
- 计划创建、笔记与资料入口从 `ChatHome` 派生，不再作为首层 Tab 分发。

---

## 三、数据结构

### 3.1 Task（Now 页任务）

```typescript
export type TaskCategory = 'work' | 'study' | 'life' | 'health';
export type TaskPriority = 'low' | 'medium' | 'high';
export type TaskStatus = 'pending' | 'in-progress' | 'completed';

export interface Task {
  id: string;
  title: string;
  description?: string;
  category: TaskCategory;
  priority: TaskPriority;
  status: TaskStatus;
  estimatedMinutes?: number;
  date: string;           // "YYYY-MM-DD"
  createdAt: string;      // ISO 8601
  completedAt?: string;   // ISO 8601
}
```

### 3.2 AppState（应用状态）

```typescript
export type EnergyLevel = 'low' | 'medium' | 'high';
export type AppMode = 'light' | 'normal' | 'intensive';

export interface AppState {
  energy: EnergyLevel;
  mode: AppMode;
  aiModel: string;  // 'gpt-4o' | 'gpt-3.5-turbo' | 'claude-3'
}
```

### 3.3 CustomTask / CustomPhase / CustomPlan（自定义计划）

```typescript
export type TaskType = 'sequential' | 'recurring';

export interface CustomTask {
  id: string;
  title: string;
  taskType: TaskType;
  done: boolean;
  // sequential 模式
  startDate?: string;       // "YYYY-MM-DD"
  startTime?: string;       // "HH:MM"
  endTime?: string;         // "HH:MM" — 若设置则覆盖 duration
  duration?: number;        // 分钟
  // recurring 模式
  recurringDays?: number[]; // 0=Sun...6=Sat
  recurringTime?: string;   // "HH:MM"
}

export interface CustomPhase {
  id: string;
  name: string;
  description: string;
  period: string;
  status: 'upcoming' | 'active' | 'completed';
  tasks: CustomTask[];
}

export interface CustomPlan {
  id: string;
  name: string;
  type: string;
  icon: string;
  color: string;
  gradFrom: string;
  gradTo: string;
  startDate: string;
  endDate?: string;
  isLongTerm: boolean;
  dailyMinutes: number;
  phases: CustomPhase[];
  tasks: CustomTask[];   // 计划级别任务（不属于任何阶段）
  createdAt: string;
  updatedAt: string;
}
```

### 3.4 UserProfileData（个人信息）← v1.1 NEW

```typescript
export interface UserProfileData {
  name: string;
  bio: string;
  city: string;
  avatar: string;     // emoji 字符
  avatarBg: string;   // hex 颜色
  tags: string[];
  createdAt: string;
  updatedAt: string;
}
```

### 3.5 Track / Phase / DailyTask（年度作战地图）

```typescript
export interface DailyTask {
  id: string;
  title: string;
  done: boolean;
  taskType?: TaskType;
  duration?: number;
  startDate?: string;
  startTime?: string;
  endTime?: string;
  recurringDays?: number[];
  recurringTime?: string;
}

export interface Phase {
  id: string;
  name: string;
  period: string;
  status: 'completed' | 'active' | 'upcoming';
  objective: string;
  weekGoal: string;
  todayTasks: DailyTask[];
}

export interface Track {
  id: string;
  name: string;
  emoji: string;
  tagline: string;
  color: string;
  gradFrom: string;
  gradTo: string;
  bgLight: string;
  progress: number;       // 0-100
  phase: string;          // 当前阶段名称
  phaseNumber: number;
  totalPhases: number;
  status: 'active' | 'blocked' | 'idle';
  suggestion: string;
  rhythm: number[];       // 最近7天强度 0-3
  target: string;
  deadline: string;
  phases: Phase[];
}
```

### 3.6 NoteFolder / NoteFile（笔记模块）← v1.2 NEW

```typescript
export type NoteFileFormat = 'document' | 'markdown';

export interface NoteFolder {
  id: string;
  name: string;
  parentId?: string | null; // null = 根目录
  createdAt: string;
  updatedAt: string;
}

export interface NoteFile {
  id: string;
  title: string;
  folderId?: string | null; // null = 根目录
  format: NoteFileFormat;
  content: string;
  createdAt: string;
  updatedAt: string;
}
```

---

## 四、localStorage Keys

| Key | 类型 | 说明 |
|-----|------|------|
| `now_tasks` | Task[] | Now 页任务列表 |
| `now_messages` | ChatMessage[] | Chat 消息历史 |
| `now_state` | AppState | 能量/模式/AI模型 |
| `now_initialized` | string | 初始化标志 |
| `custom_plans` | CustomPlan[] | 自定义计划列表 |
| `notes_folders` | NoteFolder[] | 笔记文件夹结构 |
| `notes_files` | NoteFile[] | 笔记文件内容 |
| `user_profile` | UserProfileData | 用户个人信息 |

---

## 五、页面详情

### 5.1 Now 页（/）
- 展示今日任务列表，按优先级排序
- 任务卡片支持状态切换（pending → in-progress → completed）
- 顶部显示当前时间、日期和能量状态

### 5.2 Chat 页（/chat）
- 全屏 AI 对话界面，隐藏底部导航
- 支持文本消息发送
- 接入 AI 模型（当前为模拟回复）

### 5.3 Plan 页（/plan）
- 自定义计划列表（来自 custom_plans）
- 入口卡片：年度作战地图
- 快捷创建新计划按钮

### 5.4 年度作战地图（/plan/battle）
- 5 条预设主线：换工作 / 作品集 / 买房 / 恋爱 / 状态管理
- 每条显示：进度条、当前阶段、7日执行节奏、AI 建议
- 状态徽章：active / blocked / idle

### 5.5 主线详情（/plan/battle/:trackId）
- 完整阶段时间线（completed / active / upcoming）
- 当前阶段任务列表
- 顺序任务显示时间段，定期任务显示周期

### 5.6 计划编辑器（/plan/editor、/plan/editor/:planId）
- 创建/编辑自定义计划
- 添加阶段和任务
- 任务双模式：sequential（指定日期时间段）/ recurring（按周期重复）
- sequential 模式：startDate + startTime + duration 联动计算 endTime

### 5.7 Notes 首页（/notes）← v1.2 NEW
- 根目录文件夹与文件列表
- 顶部提供 `新建文件夹` 与 `新建笔记`
- 作为主页独立一级页面，底部导航高亮 Notes

### 5.8 NoteFolder 页（/notes/folder/:id）← v1.2 NEW
- 展示当前文件夹下的子文件夹与文件
- 支持继续创建子文件夹
- 支持在当前层级创建新笔记

### 5.9 NoteFile 页（/notes/file/:id）← v1.2 NEW
- 展示单个文件标题、路径与正文
- 支持普通文档与 Markdown 内容
- 同页切换 `编辑` / `预览`

### 5.10 Profile 页（/profile）
- 顶部 Hero 卡片（可点击跳转个人信息）
- 展示用户头像/昵称/座右铭/标签
- 能量水平选择（低/中/充沛）
- 执行模式选择（轻量/正常/专注）
- AI 模型选择
- 清空所有数据

### 5.11 个人信息页（/profile/me）
- 头像选择：20 个 Emoji + 8 种背景色
- 编辑昵称、座右铭、城市
- 标签系统：10 个预设标签 + 自定义标签
- 执行数据展示（总任务/已完成/完成率 + 进度条）
- 数据存入 localStorage key: user_profile

### 5.12 开屏页面（SplashScreen）
- 每次应用启动展示约 2 秒
- 深紫色渐变背景（#0f0d2e → #3730a3）
- ⚡ Logo 弹性弹入动画（spring ease）
- 4 层同心圆光晕装饰
- 旋转渐变光环（12s 循环）
- 应用名"执行力" + 副标题渐显
- 跳动加载点（三个点，stagger 延迟）
- 退出：scale(1.06) + opacity(0) 淡出

### 5.13 v2 ChatHome 对话主入口（/）

- 作为 v2 唯一一级页面，承接用户打开 App 后的首屏。
- 首次进入只展示一句主动提问：“要开启今天的计划吗？”
- 保留底部 composer：`+`、输入框、语音/发送入口。
- 点击“开启今天计划”进入 `TodayFocus`。
- 结果反馈返回后，以简短消息形式展示“已记录完成，计划进度已更新”等反馈。
- 不展示底部 Tab、完整仪表盘或旧 Now 页卡片堆叠。

### 5.14 v2 TodayFocus 全屏专注页（/today/focus）

- 二级全屏页，只突出当前一件任务。
- 顶部只保留返回/关闭、轻量标题和可选预计时长。
- 中央展示任务标题、所属计划、预计时长和极简计时/进度。
- 主操作只保留 `开始专注`；弱化 `换一个`、`稍后`。
- 不显示聊天输入框、侧边菜单、底部导航、备用任务列表、统计卡或计划摘要。

---

## 六、样式规范

### 颜色体系

| 用途 | 色值 |
|------|------|
| 主色 | #6366f1（Indigo-500）|
| 辅色 | #8b5cf6（Violet-500）|
| 强调绿 | #10b981（Emerald-500）|
| 警告黄 | #f59e0b（Amber-500）|
| 危险红 | #ef4444（Red-500）|
| 页面背景 | #f5f4ff |
| 深色背景 | #1a1740 |
| 卡片背景 | #ffffff |
| 文字主色 | #1e1b4b / #1e293b |
| 文字次色 | #64748b |
| 文字三级 | #94a3b8 |

### 圆角系统

| 场景 | 值 |
|------|-----|
| 按钮/标签 | 8px - 12px |
| 小卡片 | 16px（rounded-2xl）|
| 大卡片 | 20-24px（rounded-3xl）|
| 头像 | 22-28px |

### 阴影系统

| 层级 | 值 |
|------|-----|
| 轻度（卡片）| 0 2px 10px rgba(0,0,0,0.05) |
| 中度 | 0 4px 20px rgba(0,0,0,0.08) |
| 强调（渐变卡）| 0 8px 24px rgba(49,46,129,0.3) |
| 光晕（彩色按钮）| 0 8px 24px {color}45 |

---

## 七、动画规范

| 场景 | 配置 |
|------|------|
| 页面入场 | opacity 0→1, y 16→0, duration 0.3s |
| 列表交错 | delay stagger 0.07s |
| 弹性弹出 | ease [0.34, 1.56, 0.64, 1] |
| 按压反馈 | whileTap scale 0.92-0.97 |
| 开屏 Logo | scale 0.4→1, spring bounce |
| 旋转装饰 | rotate 360, duration 12s, linear, repeat |
| 开屏退出 | scale 1.06 + opacity 0, duration 0.5s |
| 折叠展开 | height auto + opacity, duration 0.22s |

---

## 八、已完成功能清单

- [x] 5-Tab 底部导航（Now / Chat / Plan / Notes / Profile）
- [x] Now 页 - 今日任务列表与状态管理
- [x] Chat 页 - AI 对话界面（全屏）
- [x] Plan 页 - 自定义计划卡片列表
- [x] Notes 页 - 根目录文件夹与文件列表（v1.2）
- [x] NoteFolder 页 - 子文件夹浏览与新建（v1.2）
- [x] NoteFile 页 - 文档 / Markdown 编辑与预览（v1.2）
- [x] 年度作战地图 - 5 条主线轨道
- [x] 主线详情 - 阶段时间线 + 任务勾选
- [x] 计划编辑器 - 创建/编辑计划/阶段/任务
- [x] 任务双模式 - sequential / recurring 互相联动
- [x] Profile 页 - 能量 / 模式 / AI 模型设置
- [x] 数据持久化 - localStorage
- [x] 开屏页面 - 启动动画（v1.1）
- [x] 个人信息页 - 头像/昵称/标签/执行数据（v1.1）
- [x] 产品文档页 - 可浏览可下载（v1.1）

---

## 九、Backlog

- [ ] Now 页 - 读取顺序任务，高亮"当前该做什么"
- [ ] Now 页 - 根据 recurringDays 动态生成当天定期任务
- [ ] 时间冲突检测与提示
- [ ] 任务统计图表（Recharts 周/月维度）
- [ ] AI 真实接入（OpenAI / Claude API）
- [ ] 推送通知 / 定时提醒
- [ ] 数据云同步（Supabase）
- [ ] 深色模式
- [ ] 多计划对比视图
- [ ] 任务拖拽排序
- [ ] Notes 与 Plan 的双向关联（从笔记生成计划 / 给计划挂笔记）
- [ ] Markdown 工具栏与更多块级编辑能力

---

*执行力 v1.2 · 更新于 2026/5/11*
