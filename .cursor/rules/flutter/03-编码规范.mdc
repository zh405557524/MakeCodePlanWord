---
description: Flutter 通用编码规范，强调目录化、命名、导入、状态管理与错误处理
globs: "**/lib/**/*.dart"
alwaysApply: true
---

# 编码规范

## 一、文件组织

- 使用目录化功能模块
- 使用 `index.dart` 作为模块入口
- 文件保持单一职责
- 相同功能的 `view/controller/widgets/dialog` 放在同一模块目录中

---

## 二、命名规范

- 文件和目录：`snake_case`
- 变量和函数：`camelCase`
- 类和枚举：`PascalCase`
- 常量：按项目统一规范，优先集中管理

---

## 三、导入顺序

按以下顺序组织导入：

1. Dart SDK
2. Flutter SDK
3. 第三方包
4. 项目内部包

若当前模块采用 `index.dart + part / part of`：

- 只有 `index.dart` 负责外部 `import`
- 模块内 `view/controller/widgets/dialog` 不再各自写本地依赖导入

---

## 四、状态管理

- 默认使用 `GetX`
- 页面状态放页面 Controller
- 跨页面共享状态放 `store/`
- 可复用业务动作优先下沉到 `services/`

不要：

- 在 Widget 树里偷偷维护复杂业务状态
- 在多个页面各自复制一份同样的全局状态逻辑

---

## 五、Context 规则

- 禁止在 Controller 中使用 `Get.context`
- 需要 `BuildContext` 时通过方法参数传入
- 与导航、主题、弹窗紧耦合的动作优先放在 View 层触发

---

## 六、错误处理

- 异步调用统一处理异常
- 错误信息要能回到用户可读提示
- 关键错误要进入日志
- 不允许静默吞掉关键失败

---

## 七、注释要求

至少为以下内容补注释：

- 文件职责
- 类职责
- 关键方法职责
- 状态切换原因
- 特殊路由参数说明

注释解释“负责什么/为什么这样做”，不要重复代码字面意思。

---

## 八、验证要求

- 改动后至少通过 `flutter analyze`
- 关键链路改动后补最小运行验证或测试
- 目录结构、命名与聚合方式必须保持一致
