# {项目名称} 变更计划

> 规则来源：`.cursor/rules/00-核心工作流程.mdc`  
> 用途：记录二次调整、局部修改或返工计划，控制影响范围，避免小改动触发大面积重写。  
> 维护规则：发生变更时创建或更新；变更完成后补充完成记录。

---

## 1. 变更概况

| 字段 | 内容 |
|------|------|
| 变更类型 | `small_change` |
| 变更来源 | `{用户反馈、检测报告、设计调整、编码问题等}` |
| 变更摘要 | `{一句话说明本轮变更}` |
| 当前状态 | `pending` |

变更类型：

| 类型 | 含义 |
|------|------|
| `small_change` | 局部小改，默认类型，只允许修改影响文件清单内的内容 |
| `refactor` | 局部或模块级重构，必须由用户明确要求 |
| `rewrite` | 大范围重写，必须由用户明确要求 |

---

## 2. 影响文件清单

| 文件 | 影响原因 | 处理方式 |
|------|----------|----------|
| `{项目文件路径}` | `{为什么受影响}` | `{新增/修改/检测/无需修改}` |

---

## 3. 不应改动范围

- `{明确不需要修改的文件、模块、章节或代码范围}`

---

## 4. 分步执行计划

1. `{步骤一}`
2. `{步骤二}`
3. `{步骤三}`

---

## 5. 验收标准

- `{验收标准一}`
- `{验收标准二}`
- `{验收标准三}`

---

## 6. 完成记录

| 时间 | 操作 | 修改文件 | 结果 |
|------|------|----------|------|
| `{YYYY-MM-DD}` | `{执行内容}` | `{文件路径}` | `{结果}` |
