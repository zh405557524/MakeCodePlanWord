---
description: 工作时长评估阶段 - 基于技术方案的任务拆分，给出三点估计、关键路径、里程碑与排期
globs:
alwaysApply: false
---

# 工作时长评估规则

## 通用执行契约

执行本规则前，必须先完成核心规则中的工作空间定位：

- 读取 `projects/{currentProject}/workflow.md`；若不存在，先按 `.cursor/rules/templates/workflow.md` 创建。
- 读取本规则，以及 `workflow.md` 中“当前步骤依赖文件”列出的材料。
- 执行前先形成本轮评估计划，再生成或更新 `effort-estimate.md`。
- 执行后更新 `workflow.md` 的当前阶段、状态、恢复点、依赖文件、生成文件、注意事项和执行记录。
- 若本轮是二次调整或命中变更管控关键词，先读取或创建 `projects/{currentProject}/change-plan.md`，默认按 `small_change` 限定影响范围。

## 前置条件

执行本规则前，必须确认：

1. `projects/{currentProject}/02-技术方案/tech-plan.md` 已存在
2. `projects/{currentProject}/01-需求/figma-design-plan.md` 已存在
3. 用户已明确输入「评估工时」/「估算开发时间」/「项目工作时长评估」之一，或处于 tech-plan 完成后的默认推进点

## 目标

把已经在 `tech-plan.md` 中拆分的 `T-F00x / T-B00x` 任务转化为可评审的工时估计，给出关键路径、里程碑建议日期与风险缓冲，作为后续 07/08 技术文档与 09/10 总体计划的排期依据。

## 输入材料

- `projects/{currentProject}/02-技术方案/tech-plan.md`（任务拆分与阶段规划）
- `projects/{currentProject}/01-需求/figma-design-plan.md`（页面与状态复杂度）
- `projects/{currentProject}/01-需求/requirements.md`（功能边界与优先级）
- 用户提供的团队配置与排期约束（如「3 名 Flutter + 1 名 Kotlin + 1 名 QA」「目标 GA 日期」）

## 基本原则

- 每条任务都必须给出三点估计（O / M / P），不允许只填一个数。
- 估计单位统一为「人天」，1 人天 = 6 小时有效工时（默认值，可在前提假设中调整）。
- 关键路径必须基于任务依赖图推导，不能凭直觉。
- 必须区分「单人工时」与「日历时长」：日历时长 = 单人工时 / 并行度 + 缓冲。
- 工时评估文档支持多版本（v1.0 基线，v1.x 变更增量），不允许直接覆盖历史评估。

## 输出路径

- `projects/{currentProject}/02-技术方案/effort-estimate.md`

## 固定结构

`effort-estimate.md` 至少包含以下章节：

1. **评估前提与假设**
   - 当前评估版本号（v1.0 / v1.x）
   - 团队配置（N 名 Flutter + N 名 Kotlin + N 名设计/QA）
   - 每日有效工时（默认 6 小时）
   - 并行度（前后端是否并行、单人是否多任务切换）
   - 估算单位与精度（默认 0.5 人天为最小颗粒）
2. **任务粒度**
   - 与 `tech-plan.md` 第 6 章「开发任务拆分」逐条对齐
   - 每条任务保留 ID（如 T-F001 / T-B001），名称必须与技术方案一致
3. **三点估计表**

   | 任务 ID | 名称 | 乐观 O | 最可能 M | 悲观 P | 期望 E=(O+4M+P)/6 | 标准差 σ=(P-O)/6 | 备注 |
   |---------|------|--------|----------|--------|-------------------|-------------------|------|

4. **阶段汇总**

   | 阶段 | 任务范围 | 期望工时合计 | 日历时长 | 关键里程碑 |
   |------|----------|--------------|----------|------------|
   | 前端编码 | T-F001~T-Fxxx | | | |
   | 后端编码 | T-B001~T-Bxxx | | | |
   | 联调测试 | | | | |
   | 构建打包 | | | | |
   | 上架发布 | | | | |

5. **关键路径与并行度分析**
   - 任务依赖图（用文字 / 表格描述前端任务依赖哪些后端接口）
   - 关键路径上的任务列表
   - 并行起止点（哪些任务可与他任务同时进行）
6. **里程碑与日期建议**

   | 里程碑 | 含义 | 目标日期区间 | 验收标准 |
   |--------|------|--------------|----------|
   | Alpha | 主链路打通，内部可用 | YYYY-MM-DD ~ YYYY-MM-DD | |
   | Beta | 功能完整，内部回归 | | |
   | RC | 候选发布，外部小范围 | | |
   | GA | 正式上架 | | |

7. **风险缓冲**
   - 标准缓冲：总工时 +20%
   - 强不确定性缓冲：+40%（适用于含外部依赖、未验证 SDK、复杂算法的任务）
   - 必须写明本项目采用的缓冲策略与原因
8. **资源/人力变化敏感性**
   - 多 1 人 Flutter 对总日历时长的影响
   - 少 1 人 Kotlin 对总日历时长的影响
   - 若 QA 并行度变化，对联调阶段的影响
9. **评估版本与历史**
   - 当前版本号 + 评估生成日期
   - 历史版本列表（v1.0 / v1.1 / v1.2 ...），每条注明触发原因（如「新增 CHG-007 直播分享需求」）
10. **待确认项**
    - 团队配置假设是否成立
    - 是否有外部资源依赖未排期
    - 是否存在尚未拆分到 T-Fxxx / T-Bxxx 的灰色任务

## 评估增量（v1.x）触发规则

- 每次 `change-plan.md` 完成一条变更并归档到 `change-log.md` 后，必须回到本规则追加一份「评估增量」（v1.x），仅评估新增/变化任务的工时、对里程碑日期的位移、是否触及关键路径。
- 评估增量以追加章节形式写到 `effort-estimate.md`，不覆盖 v1.0 基线。
- 若同一里程碑被多次位移，必须在「里程碑与日期建议」表中保留所有版本的目标日期区间。

## 推进规则

- 评估文档生成后必须暂停，等待用户确认时间表与里程碑。
- 用户确认通过后，再分别进入 07 前端 Flutter 技术文档与 08 后端 Kotlin 技术文档阶段；这两个阶段允许并行。
- 若用户未给出团队配置，先用合理默认值生成 v1.0 草稿，并在「待确认项」中明确列出，避免阻塞流程。
