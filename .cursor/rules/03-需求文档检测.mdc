---
description: 需求文档检测阶段。检查 PRD 是否完整、清晰、一致，并符合固定格式。
globs:
alwaysApply: false
---

# 需求文档检测规则

## 目标

检查 PRD 是否达到“可阅读、可评审、可进入下一阶段”的质量。

## 检测输入

检测时至少结合以下材料：

- `requirements.md`
- `references/` 下的文档和图片
- `detection-feedback.md`（如果存在）
- 本轮用户新增反馈

## 基本原则

- 不只读 PRD，要和原始材料对照。
- 新反馈先写入 `detection-feedback.md`，再检测。
- 优先检查是否清楚、是否一致、是否遗漏。
- 检测完成后必须暂停，等待用户确认。

## 检测维度

至少检查：

- 结构是否符合固定格式
- 范围是否清楚
- 流程是否能读懂
- 流程图是否存在且表达清晰
- 页面说明是否足够
- 页面是否按 UI 从上到下拆解
- 区块说明是否能对应页面真实结构
- 规则是否自洽
- 数据与同步说明是否合理
- 原始材料是否有遗漏
- 历史反馈是否闭环

## 交互检测重点

交互部分至少满足：

- 整体流程有流程图
- 页面级需求中的页面都有流程图
- 流程图能看出进入、操作、反馈、流向
- 文字说明不是只重复流程图标题
- 不允许只有“页面 A -> 页面 B”这类过于笼统的描述
- 复杂交互是否有单独说明
- 用户是否能从页面描述中看懂从上到下每一块在做什么

## 输出要求

输出一份简洁检测报告，至少包含：

- 总体结论：PASS / FAIL
- 主要问题
- 建议优化项
- 与原始材料的对照结论
- 与 `detection-feedback.md` 的闭环情况

## 反馈记录

用户提出修订意见时，追加写入：

- `projects/{currentProject}/01-需求/detection-feedback.md`

记录要求：

- 保留时间
- 保留本轮要点
- 不要冗长复述

## 推进规则

- PASS：等待用户明确说继续，再进入 `Figma设计稿计划` 阶段。
- FAIL：根据用户意见修改 PRD 后重新检测。
- 不允许检测完自动跳到技术方案阶段。
- PRD 通过后的默认下一步是补齐 `figma-design-plan.md`，不是直接生成技术方案。
